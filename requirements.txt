# 五子棋独立版依赖
#
# 核心引擎（纯算法）零第三方依赖：标准库即可运行。
#   依赖：仅 Python 标准库（tkinter 为自带）
#
# 以下为「可选」依赖，用于启用神经网络叶子评估 / 决策大脑：
#   - numpy    ：分支矩阵 / 决策输入编码
#   - torch    ：DecisionNet + BranchValueNet 加载与推理
# 未安装时引擎自动回退为纯算法（不会崩）。
#
# torch>=2.0
# numpy>=1.24
