# 算法设计文档

> 状态：第一至四章已按当前实现细化到函数级（每个判定条件、每个常量、每条过滤都写明）；
> 第五章路径权重/剪枝、第六章叶子评估、第七章五态聚合和第八章汇总口径已按用户确认更新；
> 其余章节仍可能调整。
>
> 本文档区分三类内容：
>
> - **代码事实**：当前实现确实如此；
> - **已知问题**：当前实现存在的问题或缺口；
> - **待确认**：需要用户最终拍板的设计点，未确认前不得按文档直接改代码。

---

# 第一章 总览

## 1.1 目的与范围

本文档描述五子棋 AI 的完整算法链：

```text
棋型识别
  -> 候选点选择
  -> 深度推演
  -> 叶子评估
  -> 强制胜负判定
  -> 候选点分数汇总
  -> 最终落子选择
```

覆盖模块：

```text
pattern -> search -> deep_search -> engine
```

不覆盖：

- 棋局规则、会话、GUI、记忆、插件适配层；
- 对局存档格式；
- 神经网络训练流程（只描述接口和输入）。

## 1.2 总体决策链

```text
Engine.analyze_turn(board, player)          # player = 当前决策方
  ├─ part1_opp_threat   对手视角威胁分布（按父类 KILL/THREAT/POTEN 分档列出空位）
  ├─ part2_my_threat    己方视角威胁分布
  ├─ part3_candidates   初始候选
  └─ part4_result       最终结果 {type, move, ranked}
```

`part3_candidates` 的三种形态：

| type / reason | 产生条件 | `points` 形态 |
|---|---|---|
| `direct` + `my-five` / `opp-five` | 自己有成五点 / 对方有成五点 | `[(r,c), …]`，**直取第一个，不深推** |
| `direct` / `defense` + `my-vcf` / `my-d33` / `opp-vcf` / `opp-d33` | `tactical_candidates` 命中 | `my-*` 为列表（补 100 权重）；`opp-*` 为 `{(r,c): 权重}` 字典 |
| `fallback` | 战术类目全空 | `{(r,c): 综合分}`：`_candidate_score` 叠加记忆修正，再夹到 `[1, 100]` |

`part4_result` 的两种形态：

- **不深推**：`my-five` / `opp-five`（落子即胜，或必须直接堵），或候选**唯一**
  （列表/字典都只剩一个点）→ 该点给 `score = 1e9`，`ranked` 只含这一条；
- **深推**：其余情况全部交给 `DeepSearch.rank_candidates` 逐个打分，
  `ranked` **全返回、不截断**，`move` 取排序后第一名。

最后一步：可选的决策网络（默认关闭）可在 `ranked` 内另选一个点覆盖 `move`；
纯算法路径下 `move` 恒等于 `ranked[0]`。

## 1.3 核心术语

| 术语 | 含义 |
|---|---|
| 父类线棋型 | 单方向扫描得到的 10 类线棋型（本文简称"父类"） |
| 档位 | `DIR_LEVEL`：父类 → 0~5，越小越强；四方向档位组合出点棋型 |
| 152 子类 | `pattern` 层线棋型的细分类（父类 ID + 父类内序号），只描述**线** |
| 18 子类 | `search` 层每个空位的点棋型编号 0~17，描述**点** |
| 双空跳 | 两个同色子间隔恰好两空（`x__x`），由端类型 5 触发的合成判定，见 2.7 |
| 棋段区 | 增量刷新的范围：段内空缝 + 棋段两端尾部空位全取，见 3.8 |
| 攻防档位（gear） | 1攻4防 ~ 4攻1防，按双方 A1/A2/B 压力与杀势动量选择，见 4.5 |
| 候选点 | 准备进入搜索或直接选择的落子位置 |
| 根候选 | `rank_candidates` 的顶层候选 |
| 分支 | 从根候选开始的一串模拟落子 |
| 叶子 | 温度耗尽、成五或没有候选时的终止节点 |
| 路径权重 | `path_w`，候选步质量的连乘衰减 |
| 五态 | `+1/+2/0/-2/-1` 强制胜负状态 |
| 叶子分 | 叶子节点的评估分数 |
| 分支分 | 叶子分按路径权重回溯得到的根候选分数 |

## 1.4 模块职责

| 模块 | 职责 |
|---|---|
| `pattern.py` | 单方向线棋型（10 父类 + 152 子类）、四方向点棋型汇总 |
| `search.py` | 黑白状态表与增量刷新、18 子类合成、候选生成、攻防档位 |
| `deep_search.py` | 温度银行、递归、剪枝、叶子评估、五态判定 |
| `engine.py` | 组织候选、调用深推、输出最终选点 |

## 1.5 文档标记约定

- **代码事实**：与当前代码一致。
- **已知问题**：当前代码的缺口，后续可能修改。
- **待确认**：需要用户确认的算法设计决策。

---

# 第二章 单方向线棋型的 10 类父类判定

## 2.1 两个扫描器（先分清）

线棋型由两个**完全不同**的扫描器支撑，名字相近但行为不同：

| | `pattern._scan_two` | `SearchScope._scan_side` |
|---|---|---|
| 用途 | 棋型判定（喂给 `_classify`） | 增量更新的范围计算 |
| 调用方 | `analyze_direction`（→ `analyze_point` / `_eval`） | `_segment_scope` |
| 起点 | 基子（假定已落子） | 落子点 |
| 传入方向 | `(dr, dc)`，函数内部同时扫正反两侧 | `(dr, dc)` + `sign`，每次只扫一侧 |
| 返回 | 数值：`stones` / 缝 / 端类型 | 坐标：段内己方子 / 段内缝 / 段尾空位 + 终止原因 |
| 终止 | 对方子或盘外、**连续两空**（停前前瞻一格） | 对方子或盘外、**连续三空** |
| 额外截断 | 有：第二缝、4 连截断 | 无 |
| 基子 | 计入（`stones` 初值 1） | 不计入 |

**两者停止阈值不同（两空 vs 三空）是后续多处推导的前提**：范围计算比棋型判定
看得更远，因此"棋型判定能读到的格子"一定也在范围计算的视野内（见 3.8）。

## 2.2 `_scan_two` 的双侧状态机

`_scan_two(board, r, c, player, dr, dc)` 以基子 `(r,c)` 为中心，沿 `(dr,dc)` 的
正、反两侧**各自独立**推进（`r_bull` / `l_bull`），每轮 `i` 加 1。

每侧维护：`gap`（本侧是否正处于缝中）、`gap_count`、`gap_location`（第一个缝到基子的
距离）、`gap_seen` / `contig`（首个缝前是否出现过己方子 / 连了几颗）、`edge = [位置, 类型]`。

每侧遇到一格时：

| 该格是 | 处理 |
|---|---|
| 己方子 | `stones += 1`；若本侧处于缝中 → `gap_count += 1` 并合上缝；随后判定截断条件 |
| 空位 | 本侧**不在**缝中 → 置 `gap = True`，继续；本侧**已处于**缝中（即连续两空）→ 停止，并对再前一格做**前瞻**决定端类型 1 还是 5 |
| 对方子或盘外 | 停止，端类型 3（若处于缝中）或 2 |

两个截断条件（都在"己方子合上缝"的那一刻判定）：

- **第二缝**：`gap_count >= 2` —— 一条线最多计入一个缝；
- **4 连**：`l_contig + r_contig + 1 >= 4` —— 落点两侧的连续段加起来已够 4 连，
  缝外的子不再并入（否则会把活四误判成带缝眠四，漏杀）。

命中任一条：端类型置 4、位置置缝的位置、`stones -= 1`（刚计入的那颗子退回），本侧停止。

## 2.3 端类型 1~5

| 类型 | 图形 | 产生条件 | 位置字段 |
|---|---|---|---|
| 1 | `__` | 连续两空，且再前一格**不是**己方子 | 第一个空位的距离 |
| 2 | `\|` | 直接撞上对方子或盘外（中间无空位） | 墙的距离 |
| 3 | `_\|` | 先遇一个空位、再撞墙 | 空位的距离 |
| 4 | 断裂端 | 第二缝截断或 4 连截断 | 缝的位置 |
| 5 | `__x` | 连续两空，且再前一格**是**己方子 | 第一个空位的距离 |

类型 1 与 5 的唯一区别就是前瞻的那一格。**类型 5 是"远端双空跳"的全部信号来源**
（见 2.7）。在 `_classify` 里 1/3/4/5 同属活端（`OPEN_END`），类型 2 是唯一的堵端。

## 2.4 `_overall_judge`：双缝跳三

`stones == 3` 且两侧各有一个缝（`l_gap == 1 and r_gap == 1`）→ 直接判 `SLEEP3`，
即 `x_X_x` 双缝跳三。填任一缝都只成冲四，所以整体恒为眠三，**与两端类型无关**。

该判定在 `_normalize` **之前**执行；命中时该方向只产出一条记录，`gap_location` 记为 1。

## 2.5 `_normalize`：单缝 / 双缝 → 1~2 条记录

- 缝总数 ≤ 1：直接产出一条，`gap_location` = 该缝的位置（无缝为 0）；
- 两侧各一缝：拆成两条（左段+中心段 / 中心段+右段），基子一侧的缝标为断裂端 4：

```text
left_seg  = (l_edge[0] - 1) - l_gap_location
right_seg = (r_edge[0] - 1) - r_gap_location
mid       = stones - left_seg - right_seg

记录1：stones = left_seg + mid,  gap_location = l_gap_location,  r_edge 改标为 [r_gap_location, 4]
记录2：stones = mid + right_seg, gap_location = r_gap_location,  l_edge 改标为 [l_gap_location, 4]
```

## 2.6 `_classify`：10 父类判定链

输入是 `_normalize` 产出的单条记录 `(stones, gap_location, l_edge, r_edge)`。
先算辅助量：

```text
gap     = 1 if gap_location != 0 else 0
wall_l  = l0 + (1 if lt == 3 else 0)        # 类型 3 的墙在空位之后一格
wall_r  = r0 + (1 if rt == 3 else 0)
seg_len = wall_l + wall_r - 1               # 墙到墙的段长
OPEN_END = (1, 3, 4, 5)                     # 活端集合；类型 2 是唯一堵端
```

按**代码顺序**（顺序即优先级）判定，命中即返回：

| 顺序 | 条件 | 结果 |
|---:|---|---|
| 1 | `lt in (2,3)` 且 `rt in (2,3)` 且 `seg_len < 5` | `DEAD` |
| 2 | `stones == 1` | `SINGLE` |
| 3 | `stones >= 5` 且 `gap == 0` | `FIVE` |
| 4 | `stones == 4` 且 `gap == 0` 且两端都在 `OPEN_END` | `LIVE4` |
| 5 | `stones == 4`（其余情况） | `SLEEP4` |
| 6 | `stones > 4`（带缝的 5 子以上） | `SLEEP4` |
| 7 | `stones == 3` 且 `gap == 0` 且（一端 in (1,4,5)、另一端活端） | `LIVE3` |
| 8 | `stones == 3` 且 `gap == 1` 且两端都活 | `LIVE3` |
| 9 | `stones == 3`（其余） | `SLEEP3` |
| 10 | `stones == 2` 且（一端 in (1,4,5)、另一端活端） | `LIVE2` |
| 11 | `stones == 2`（其余） | `SLEEP2` |
| 12 | 其余 | `NONE` |

三处最容易误改：

- **`DEAD` 排在 `SINGLE` 之前** —— "两端都堵且段长 < 5"优先于"单子"；
- **类型 4（断裂端）当活端用** —— 它是第二个缝的断口，语义上等价于活端；
- **活三/活二要求至少一端是 (1,4,5)**（"真活端"），不允许两端都靠 3（半活端）互相顶
  —— 这就是 3·3 被排除出 `LIVE3` / `LIVE2` 的全部原因。

## 2.7 端 5 缝合：`_synth_double_gap`（远端双空跳）

类型 5 单独出现时**不参与** `_classify`：`analyze_direction` 有一段后处理，
同时满足三条时把该方向唯一的 `SINGLE` 记录丢掉，改走合成：

1. 该方向只剩一条记录（`len(out) == 1`）；
2. 该记录父类是 `SINGLE`（基子在这个方向上孤身一人，`stones == 1`）；
3. 左端或右端类型为 5。

合成过程 `_synth_double_gap(board, r, c, player, dr, dc, side)`：

```text
d        = 该端的位置（= 第一个空位的距离）
远端子   = 基子 + (d + 2) * 方向        # 跨过两个空位的那颗己方子
校验     ：越界或该格不是己方子 → 合成失败（SINGLE 已被丢弃，该方向无记录）
res2     = _scan_two(board, 远端子, player, dr, dc)   # ← 从搭档再扫一次
近端口   = 基子的外侧端
远端口   = 搭档的外侧端
```

按两个外侧端口定级：

| 端口情况 | parent | 子类 |
|---|---|---|
| 两端都不是 2 | `LIVE2` | `live2_j2_*`（10 个） |
| 恰一端是 2 | `SLEEP2` | `sleep2_j2_*`（4 个） |
| 两端都是 2 | `DEAD` | `dead` |

记录带 `gap_location = 2`、`double_gap = True`。四个要点：

- 距离恒为 **3**（`d + 2`，最简情形 `d = 1`）——因为条件 2 要求基子在这个方向孤立；
- **只有合成出 `LIVE2` 才会改变档位**（4）；`SLEEP2` / `DEAD` 都是档位 5，与原
  `SINGLE` 同级。所以"双空跳生效"= 两端外侧端口都不是 2；
- 合成可能返回空，此时该方向一条记录都没有，调用方按档位 5 处理；
- 合成要**从搭档再扫一次**，所以搭档外侧端口的取值依赖搭档附近的棋子 ——
  这就是 3.8 里"二阶耦合"的来源。

两侧同为类型 5 时（`x__x__x`，基子孤立且在中间），两个 `if` 分支会各合成一条记录，
该方向最终取两者中档位更优的那个。

## 2.8 与档位的关系

`search.DIR_LEVEL` 把父类映射为 0～5 档：

```text
FIVE   -> 0
LIVE4  -> 1
SLEEP4 -> 2
LIVE3  -> 3
SLEEP3 -> 4
LIVE2  -> 4
SLEEP2 -> 5
DEAD   -> 5
SINGLE -> 5
NONE   -> 5
```

## 2.9 热路径档位查询（`Search._direction_level`）

`Search._eval` 是全引擎最热的函数：一次深推决策约调用 6 万次，每次都要回答
「该点在四个方向上分别形成哪一档」。它走**档位路径**，不走记录式路径：

| 路径 | 产出 | 用途 |
|---|---|---|
| `analyze_direction` / `analyze_point` | 完整棋型记录（含子类名、`gap_sides`） | 对外接口、防守反推、候选签名 |
| `Search._direction_level` | 仅该方向的档位 0～5 | `_eval` 专用 |

两条路径对档位的影响必须严格一致：状态表编号直接决定候选分、攻防档位和叶子评估，
一旦不一致就会静默改变选点与评分。

等价性依据：

1. `parent` 是 `(stones, gap_location, l_edge, r_edge)` 的纯函数（`_classify`）。
   档位路径省略的 `sub_id`、`sub_name`、`gap_sides` 都不在它的输入集里
   （`gap_sides` 仅供 `_subclass` 区分边跳/中跳子类）。
2. 落子点在某个方向的**两侧 3 格内都没有己方子**时，该方向必为档位 5，可直接返回。
   依据：`_scan_two` 遇「连续 3 空 / 对方子 / 边界」即停，且 `stones` 初值为 1；
   某侧 3 格内无己方子时该侧必在 `i<=3` 内结束、`stones` 保持 1，
   而 `_classify` 对 `stones==1` 只返回 `SINGLE` 或 `DEAD`，两者档位都是 5；
   同时该侧端类型不可能是 5，端 5 缝合分支也不会触发。
   - 必须查**两侧**：`_scan_two` 是双向扫描（只查单侧会在随机局面上错约 5%）；
   - 必须查到**第 3 格**：双空前瞻会读距离 3，那里有己方子会合成 `LIVE2`（档位 4）。
3. 端 5 缝合（`_synth_double_gap`）不能省：它把 `SINGLE`（档位 5）换成
   `LIVE2`（档位 4），而档位 4 在 `_classify_levels` 里要计入 B 的数量。
   合成记录可能为空，此时档位保持 5。

档位路径不改变任何判定，只省略不影响档位的计算。本机实测（两个中盘深推局面，
`T0=160`）：单点求值 20.5µs → 6.9µs，单步决策 3.47s → 2.20s，叶子数与选点不变。

回归：`gomoku/tests/test_pattern_level_fast.py`。

---

# 第三章 四方向组合的 18 子类点棋型判定

## 3.1 `analyze_direction`：一个方向 → 1~2 条记录

```text
_scan_two（2.2）
  → _overall_judge（命中 → 只产出一条 SLEEP3，跳过后面两步）
  → _normalize（1~2 条）
  → _classify（每条定父类，2.6）
  → 端 5 缝合后处理（2.7；可能把唯一的 SINGLE 换成合成记录，也可能把该方向清空）
```

每条记录含：`stones` / `gap_location` / `l_edge` / `r_edge` / `parent` / `dir` /
`gap_sides`，以及子类的 `sub_id` / `sub_name`（3.7）。

`gap_sides` 只用于区分"边跳 / 中跳"的子类编码，**不参与父类判定**。

## 3.2 `analyze_point`：四方向汇总

对空位模拟落子后，四个方向各调一次 `analyze_direction`，把记录拼成一个扁平列表。

注意：`Search._eval` **不**走这个接口（它走 2.9 的档位路径）。
仍在用 `analyze_point` 的地方：对外接口、防守反推 `_defense_candidates`、
候选去重签名 `_pattern_sig`。

## 3.3 四方向档位

每个方向取该方向所有记录的 `DIR_LEVEL` **最小值**（数值越小越强），得到 4 个档位值。
某方向若没有任何记录（端 5 合成失败），该方向取默认值 5。

## 3.4 `_classify_levels`：档位组合 → 18 子类编号

```text
n0   = 档位 0（FIVE）的方向数        bcnt = 档位 4（SLEEP3 / LIVE2）的方向数
n1   = 档位 1（LIVE4）的方向数
n2   = 档位 2（SLEEP4）的方向数
n3   = 档位 3（LIVE3）的方向数
```

按顺序命中即返回：

```text
n0 >= 1              -> 0                 five
n1 >= 1              -> 1                 live4
n2 >= 2              -> 2                 d44     （双向冲四）
n2 >= 1 且 n3 >= 1   -> 3                 d34     （三四）
n3 >= 2              -> 4                 d33     （双活三）
n2 == 1              -> 5  + (3 - bcnt)   a1 系   （单冲四 + 其余维的 B 数）
n3 == 1              -> 9  + (3 - bcnt)   a2 系   （单活三 + 其余维的 B 数）
其余                 -> 17 - bcnt         b 系（4B→13 … 0B→17）
```

`bcnt` 数的是**其余三个方向**里档位为 4 的个数（档位 5 是"眠二 / 无用"，不计）。
注意判定顺序：组合优先于单档（两个眠四算 `d44` 而不是"单冲四"）。

## 3.5 18 子类表

| 编号 | 名称 | 父类家族 |
|---:|---|---|
| 0 | `five` | KILL |
| 1 | `live4` | KILL |
| 2 | `d44` | KILL |
| 3 | `d34` | KILL |
| 4 | `d33` | KILL |
| 5 | `a1_bbb` | THREAT |
| 6 | `a1_bbc` | THREAT |
| 7 | `a1_bcc` | THREAT |
| 8 | `a1_ccc` | THREAT |
| 9 | `a2_bbb` | THREAT |
| 10 | `a2_bbc` | THREAT |
| 11 | `a2_bcc` | THREAT |
| 12 | `a2_ccc` | THREAT |
| 13 | `b_bbb` | POTEN |
| 14 | `b_bbc` | POTEN |
| 15 | `b_bcc` | POTEN |
| 16 | `b_ccc` | POTEN |
| 17 | `none` | USELESS |

## 3.6 18 → 4 父类家族

```text
KILL     : 0-4
THREAT   : 5-12
POTEN    : 13-16
USELESS  : 17
```

## 3.7 152 子类表（`pattern` 层的第二层编码）

`pattern.py` 另有一套**更细**的线棋型编码，只在线棋型层面使用
（`analyze_direction` 产出的 `sub_id` / `sub_name`）。它和 `search.py` 的 18 子类
**不是同一个体系**——两者都叫"子类"，但一个描述线、一个描述点，改动时不要混。

双层编码：**父类 ID（0~9）+ 父类内部的子类序号**。父类 ID 固定为：

```text
0=FIVE  1=LIVE4  2=SLEEP4  3=LIVE3  4=SLEEP3  5=LIVE2  6=SLEEP2  7=DEAD  8=SINGLE  9=NONE
```

各族数量（`_build_subclass_table` 末尾有 assert 校验总数）：

| 父类 | 数量 | 内部划分 |
|---|---:|---|
| `FIVE` | 1 | `five` |
| `LIVE4` | 10 | 端型对**无序**（1/3/4/5 的二元组合，`live4_11` … `live4_55`） |
| `SLEEP4` | 44 | 连四 4（`sleep4_c_*`）+ 边跳 25（`sleep4_e_*`，端型**有序** 5×5）+ 中跳 15（`sleep4_m_*`，无序） |
| `LIVE3` | 25 | 连三 9（`live3_c_*`，无序，已排除 3·3）+ 跳三 16（`live3_j_*`，有序） |
| `SLEEP3` | 28 | 连三 4（`sleep3_c_*`）+ `sleep3_33` + 双缝 15（`sleep3_d_*`，无序）+ 单缝 8（`sleep3_s_*`，有序） |
| `LIVE2` | 28 | 连二 9 + 跳二 9（均无序，排除 3·3）+ 双空跳 10（`live2_j2_*`） |
| `SLEEP2` | 13 | 连型 4（`sleep2_c_*`）+ 跳型 5（`sleep2_j_*`，含 `sleep2_j_33`）+ 双空跳 4（`sleep2_j2_*`） |
| `DEAD` / `SINGLE` / `NONE` | 各 1 | `dead` / `single` / `none` |

总数 = **138（端型 1~5 的全分类）+ 14（双空跳）= 152**。

编码规则（`_subclass`）：

- **端型对的有序性**：`gap == 0` 的连型、双缝、中跳、双空跳用 `_unordered_pair`
  （镜像去重）；边跳（`sleep4_e_*`）、跳三（`live3_j_*`）、单缝跳三（`sleep3_s_*`）
  用 `_ordered_pair`（左右不可互换，因为缝偏在哪一侧会影响延伸方向）。
- **边跳 / 中跳的分界**：`_gap_sides` 算出缝两侧各有多少子，`min(左右) > 1`
  记为中跳（`m_`），否则记为边跳（`e_`）。这就是 `gap_sides` 唯一的用途。
- **双空跳**：只在 `gap_location == 2` 且带 `double_gap` 标记时出现（2.7 的合成路径）。
  注意 `live2_j2_33` 存在而 `sleep2_j2_33` 不存在——两端都是半活端（3·3）时按 `LIVE2` 判。
- **3·3 的处理**：连三/连二的活型判定要求至少一端是"真活端"，
  所以 `LIVE3` / `LIVE2` 的连型族里没有 `33`；而 `sleep2_j_33` 有（跳型不适用该规则）。

## 3.8 状态表

`Search.sb`、`Search.sw` 分别保存黑白双方每个空位“如果在此落子”的 18 子类编号。

- 真实落子和模拟落子后做局部增量更新，范围就是**棋段区**：
  落子点所在棋段（close / 连续三空为界的段内全部空缝）+ 段**两端**外侧尾部空位**全取**。
  `_scan_side` 遇连续三空即停，所以尾部最多 3 格，"全取"即 ≤3 格。
- **该范围是增量更新的唯一来源。** 把两端尾部取满之后，增量结果与 `rebuild_all`
  逐格一致（实测单步漂移 0、24 局累计漂移 0）。因此：
  - 历史上的"定点区"（切比雪夫半径 2 邻域）已删除——它覆盖的格子里，
    真正会被本手影响的都在棋段区两端尾部内，其余 8 格是"马步"位置，与落子点不共线；
  - 历史上的"状态区"（按 KILL/THREAT 筛格、空则降级 POTEN）已删除——范围完备后
    它只剩重复劳动，去掉后两张表逐格不变，而深推决策耗时约减半；
  - 历史上的"段1/段2/段3"按落子方分叉的调度已删除，黑白两条路径完全同构。
- 深推模拟使用 journal 成对回滚；
- 悔棋或重建棋局使用 `rebuild_all` 全量重算。
- 为什么必须取到尾部第 3 格：`_scan_two` 的双空前瞻最多往落子点方向看 2 格，
  于是"会被本手影响、却落在尾部第 3 格"的格子有两类——
  `● _ _ □`（□ 的双空跳搭档就是本手）与 `□ _ _ x ●`（□ 的搭档是 x，
  本手贴着搭档外侧，改的是搭档的外侧端口）。取 2 会漏掉这两类。

---

# 第四章 候选点选择

## 4.1 `Search.candidates`

对**全盘**做 6 遍扫描（黑/白 × 五连/VCF/双三），只取空位，按状态表里的
**精确子类编号**筛（不是按父类）：

| 键 | 状态表 | 子类编号 | 含义 |
|---|---|---|---|
| `w_five` / `b_five` | `sw` / `sb` | `(0,)` | 落子即成五的点 |
| `w_vcf` / `b_vcf` | `sw` / `sb` | `(1, 2, 3)` | 活四 / 双冲四 / 三四 |
| `w_d33` / `b_d33` | `sw` / `sb` | `(4,)` | 双活三 |

a1 系（5-8，单冲四）**不在** `vcf` 里（见 4.6.1）。六个键都返回完整列表，不截断。

## 4.2 `Search.tactical_candidates`

按固定顺序**命中即止**：第一个非空的类目直接决定返回，后面的类目不再检查。

```text
my-five → opp-five → my-vcf → opp-vcf → my-d33 → opp-d33
```

| 命中 | 返回 |
|---|---|
| `my-*`（five / vcf / d33） | `{(点): 100.0}`，点数不限 |
| `opp-five` | `{(点): 100.0}`（必须直接堵） |
| `opp-vcf` / `opp-d33` | 逐点跑 `_defense_candidates` 反推堵点；多个威胁点之间**按位取权重最大值**合并 |
| 全空 | `(None, {})`，交给 `_fallback` |

## 4.3 `_defense_candidates`

"对方在这里要成杀 → 我该堵哪里"。对传入的每个威胁点：

1. 把威胁方 `player` 临时摆到 `(r, c)`。**扫描期间它必须留在盘上**——否则紧贴威胁点的
   缝会被当成空位，导致漏掉 F9/G9 一类防守点（`test_regressions` 里有对应回归）；
2. 中心点本身 = **100 分**（占掉落点即破坏杀型）；
3. 对每条 `LIVE4` / `SLEEP4` / `LIVE3` 记录，沿 `l_edge`（负方向）与 `r_edge`（正方向）
   取**端空位**，权重 = **80 分**，但有两道过滤：
   - `d <= 1` 跳过：端紧贴成形点、中间没有威胁方的子，不是有效堵点；
   - 端类型 ∉ (1, 3, 4, 5) 跳过：与 `_classify` 的 `OPEN_END` 保持一致；
4. 再按 `gap_location` 取**缝位**（两个威胁方子之间的空位），同为 80 分；
   取之前要验证缝两侧紧邻格都是威胁方的子（兼容双缝拆分出来的记录）；
5. 返回 `{(r, c): 权重}`，并清除临时子。

`is_kill` 参数留了 60 分的分支（用于活三防守），但**当前没有任何调用方传 False**，
实际只用 100 / 80 两档。

## 4.4 `Search._fallback`

无必杀时的普通候选。`gear=None` 时固定 3 攻 2 防，传 gear 时按档位取 (攻, 防) 配比。

候选池 = 该方状态表里父类为 `THREAT` 或 `POTEN` 的空位。

综合分（0~100）：

```text
_candidate_score = ( TYPE_SCORE[子类]
                   + (_neighbor_score / NEIGHBOR_NORM) * W_NEIGHBOR
                   + _dual_score )
                   / SCORE_MAX * 100
```

| 项 | 取值 |
|---|---|
| `TYPE_SCORE` | a2 系 200/170/130/80；a1 系 190/160/120/70；b 系 120/90/60/30 |
| `_neighbor_score` | 自家邻近度 × w_self + 对家邻近度 × w_opp；`attack (110,-10)` / `defend (30,70)` / `dual (60,40)` |
| 邻近度 | 周围 2 格内的棋子，按「距离：紧贴 1.0 / 隔格 0.5」×「方向：同线 2.0 / 异线 0.5」加权求和 |
| `_dual_score` | 该点黑白双方父类**都是 `THREAT`** 时 +30，否则 0（一侧威胁一侧潜力不算） |
| 常量 | `NEIGHBOR_NORM = 600`、`W_NEIGHBOR = 30`、`SCORE_MAX = 260` |

取点顺序（每个列表各走一遍）：

```text
1. 打分：若该点 _dual_score > 0 用 'dual' 权重，否则用列表语义传入的 mode
   （进攻列表用 'attack'、防守列表用 'defend'——黑白对称，不按颜色硬编码）
2. 按综合分降序
3. 等效去重：局面有对称性 → 按对称轨道（D4 里使局面不变的那些变换）；
   非对称局面 → 按 _pattern_sig 棋型签名；每类只保留最高分的一个
4. 取前 n 个
然后 attack 取 N 个，defend 从「排除已入选攻点」的剩余点里取 M 个，拼成 attack + defend
```

记忆修正**不在这里做**：`move_memory_bonus()` 由 `engine.analyze_turn` 在把
fallback 候选汇入 `part3_candidates` 时叠加，并夹到 `[1, 100]`。

## 4.5 攻防档位

`_choose_gear` 只应在**双方都无 0 级**时调用（0 级由上层必杀判定单独处理）。

压力分：

```text
my_pressure  = my_a1 * 4.0 + my_a2 * 2.0 + my_b * 0.5
opp_pressure = opp_a1 * 4.0 + opp_a2 * 2.0 + opp_b * 0.5
score        = (my_pressure - opp_pressure) + zero_momentum
```

- a1 = 冲四子类 **5-8** 的空位数、a2 = 活三 **9-12**、b = 潜力 **13-16**；
- `zero_momentum` = 最近 `GEAR_ZERO_WINDOW = 10` 手内"**新出现** 0 级杀势"的次数差
  × `GEAR_W_ZERO = 5.0`（同一杀势连续存在只记一次，用 `zero_active` 去重）。

先判四条**硬约束**（不等待滞回，立即生效）：

```text
opp_a1 >= 2 且 my_a1 == 0                     -> 1 档（1攻4防）
my_a1  >= 2 且 opp_a1 == 0                    -> 4 档（4攻1防）
opp_a1 + opp_a2 >= 3 且 my_a1 + my_a2 <= 1    -> 1 档
my_a1  + my_a2  >= 3 且 opp_a1 + opp_a2 <= 1  -> 4 档
```

否则按阈值映射，再走**滞回**：

```text
score >= 2.0  -> 目标 4 档
score >= -1.0 -> 目标 3 档
score >= -3.0 -> 目标 2 档
否则           -> 目标 1 档
滞回：相对当前档位每次最多升或降一档（避免临界抖动）
```

`_target_gear` 是同一套计算，但**不写回 `self.gear`、不做滞回**，
只用于深推上下文里的档位特征记录，避免污染真实档位状态。
`GEAR_ENABLED = False` 时两者都退回 `DEFAULT_GEAR = 3`。

## 4.6 已知问题

**已知问题 4.6.1**
`candidates()` 的 `vcf` 只包含子类 1/2/3，a1 单冲四（子类 5-8）不进入 `w_vcf`。

**已知问题 4.6.2**
`_defense_candidates` 对 `d <= 1` 的近端空位直接跳过，导致活四近端防守点缺失。

**已知问题 4.6.3**
`opp-vcf` 分支只返回对方威胁的防守点，不保留当前方自己的 a1 先手点。

**已知问题 4.6.4**
`_fallback` 按静态 `_candidate_score` 取前 3 攻 2 防，可能漏掉静态分低但深推是胜着的点。

## 4.7 待确认

- 候选点选择是否按“所有威胁”重新设计？
- 防守点生成是否要覆盖 a1/a2 的全部强制点？
- 己方先手点和对方防守点的优先级如何合并？
- 候选权重必须保留原始语义，不能静默转成等权列表。

---

# 第五章 深度推演（温度银行与权重剪枝法）

## 5.1 候选展开

`_candidate_points(board, player, n=5, plies=0)`：

1. 先调用 `tactical_candidates`；
2. 命中则取前 n 个候选，保留权重；
3. 未命中则调用 `_fallback`，取前 n 个，权重 clamp 到 `[1, 100]`。

深度收窄：

```text
回合数 rounds = plies // 2
rounds >= NARROW_ROUNDS      -> 最多 CAND_N_MID 个
rounds >= HARD_NARROW_ROUNDS -> 最多 CAND_N_DEEP 个
```

当前默认：

```text
CAND_N_FULL = 5      # 注意：该常量当前【未被引用】，调用处直接写死 5
CAND_N_MID  = 3
CAND_N_DEEP = 1
NARROW_ROUNDS = 4
HARD_NARROW_ROUNDS = 8
```

权重来源与 clamp 规则不同，不能混：

| 来源 | 权重 | 是否 clamp |
|---|---|---|
| 战术候选（`tactical_candidates`） | `my-*` / `opp-five` = 100；防守反推 = 100 / 80 | 不 clamp（本身就是 100/80） |
| 普通候选（`_fallback`） | `_candidate_score` 综合分 | **clamp 到 `[1, 100]`** |

clamp 是必需的：`_dt(w)` 在 `w > 100` 时 `(100-w)` 变负、平方后回升，耗温非单调，
温度银行会失衡。

## 5.2 温度银行

单步耗温：

```text
_dt(w) = DT_M * ((100 - w) / 100) ^ DT_POW + DT_N
```

当前参数：

```text
T0      = 160
DT_M    = 400
DT_POW  = 2
DT_N    = 3
```

一步完整回合：

```text
temp2 = temp - _dt(w) - _dt(ow)
```

其中：

- `w`：当前方落子权重；
- `ow`：对方应手权重。

`T0` 是**类属性**（`DeepSearch.T0`）：界面档位切换（`play.py` 的 `TEMP_LEVELS`
80/160/240）和 `vs_mode` 都是直接改类属性、全局生效；Android 端改的是实例属性，
两者语义不同。`gomoku/tools/ga_tune.py` 另有 `FAST_T0 = 40` 用于批量评估。

## 5.3 路径权重

每一步只在本方候选列表内归一化：

```text
我方这一步：q  = 我方候选权重 / 我方本层最高候选权重
对方应手：  oq = 对方应手权重 / 对方本层最高候选权重
```

根层以下，双方都会乘进路径权重：

```text
路径权重 = oq1 × q2 × oq2 × q3 × oq3 × ...
```

其中 `oq1` 是根候选之后对方第一次应手的质量，`q2` 是下一轮我方候选的质量。

根候选自己的 `q_root` 不进入路径权重。

特点：

- 单步质量只在本方候选列表内比较，不把双方候选合在一起取最大；
- 根层只看对方 `oq`，根层以下双方 `q` / `oq` 都乘；
- 全程走最优候选时路径权重约为 1；
- 走弱分支贡献按权重缩水；
- 深层连乘自然衰减。

## 5.4 剪枝

`_prune_branches` 按绝对路径权重剪枝：

- 根层：对方应手分支按 `oq` 剪枝；
- 中间层：按 `path_w × q` 剪枝，对方应手层再乘 `oq`；
- 绝对路径权重低于 `PRUNE_W = 0.02` 的分支剔除；
- 至少保留权重最大的一支，防止整层被剪空。

## 5.5 推演流程（根层与递归层）

**根层 `rank_candidates`**：对每个根候选依次执行

```text
1. w = points[(r,c)]（字典）或 60.0（列表）
2. 构造根层 ctx（含本层候选权重与对应 18 子类）
3. 落子 → on_move(journal=j_w, record_history=False)   # 同步棋盘与状态表
4. 成五 → win 叶子，分支状态由 _leaf_forced_state 判定
5. 否则取对方候选（_candidate_points(opp, 5, plies=len(branch))）
6. 对每个对方应手：temp2 = T0 - _dt(w) - _dt(ow)
     ├─ 应手成五     → lose 叶子
     ├─ temp2 <= 0   → stale 叶子（未判定，状态记 0）
     └─ 否则         → 递归 _recursive
7. 对方应手层聚合：_merge_fatal_states（兼容字段）+ _merge_child_states（决策用）
8. 根候选分 = Σ(对方应手质量 oq × 子树分)；根状态 = 对方层聚合状态取反
9. 回滚 journal、棋盘、branch、ctx_branch
```

**递归层 `_recursive`**：结构与根层相同，四处差别：

- 有 `cache`（命中直接返回，见 5.6）；
- 路径权重改为连乘 `path_w × q × oq`（根层只用 `oq`）；
- 候选剪枝用绝对权重 `path_w × q`（根层用 `oq`）；
- 返回 6 元组 `(分支分, 旧硬状态, n_win, n_lose, n_total, 新 5 态)`。

**计数器语义**（只喂兼容字段 `fatal_ratio`，不参与排序）：

| 名字 | 含义 |
|---|---|
| `cw` / `cl` | 子树里"判定为必胜 / 必败"的节点数（统一到 main 视角） |
| `c_total` | 子树**全部**节点数（含未判定），再加当前节点本身 |
| 对方层聚合 | `opp_layer == 1` → `cl += 1`；`opp_layer == -1` → `cw += 1` |

## 5.6 cache

```text
key   = tuple((r, c) for (r, c, player, w) in branch)      # 只取坐标
value = (分支分, 旧硬状态, n_win, n_lose, n_total, 新 5 态)
```

**为什么 key 可以不带权重和温度**（改这条之前必须先确认）：

- 某一步的候选权重是「棋盘 + 已落子数」的确定性函数，而两者都由落子序列决定；
- 温度 = `T0 − Σ_dt(各步权重)`，路径权重 = 各步质量的连乘，也都是序列的函数。

所以「同序列 ⇒ 同温度、同路径权重」，缓存安全。一旦候选权重将来依赖剪枝结果、
随机数或任何序列之外的状态，key 就必须把它们一起带上。

cache 在**一次 `rank_candidates` 调用内**共享，因此不同根候选的相同后缀只推一次。

---

# 第六章 叶子评估

> 本章为已确认并已实现的叶子评估口径；实现位于 `gomoku/core/deep_search.py`，回归位于 `gomoku/tests/test_leaf_value.py`。

## 6.1 叶子触发时机

推演中某一步直接成五时，叶子状态为 `win` / `lose`，返回实际终局分 `±1000`。

温度耗尽或对方无应手时，叶子状态为 `stale`，进入本章的僵持叶子评估。

僵持叶子评估的对象是温度耗尽时的当前棋盘，即黑刚应完、轮到白走。

## 6.2 数据来源

不看历史上下文快照，直接读当前棋盘的两张状态表：

| 表 | 内容 |
|---|---|
| 白表 | 每个空点白若落子会形成的 18 子类编号 |
| 黑表 | 每个空点黑若落子会形成的 18 子类编号 |

占位格和 17 号都按 0 分处理。

> 代码按“根决策方 / 对手”对称实现：根决策方是白时，白表即根方表；根决策方是黑时角色对调，分值仍按“根方用根方基础分表、对手用对手基础分表”处理。

## 6.3 必杀判定优先级

从上往下查，命中即停。成五、强必杀、弱必杀都按“有 / 没有”判定，不数个数。

| 优先级 | 条件 | 叶子分 |
|---:|---|---:|
| 1 | 白表出现成五（0） | +920 |
| 2 | 黑表出现成五（0） | -920 |
| 3 | 白表出现强必杀（1/2/3） | +850 |
| 4 | 黑表出现强必杀（1/2/3） | -700 |
| 5 | 白表出现弱必杀（4） | +650 |
| 6 | 黑表出现弱必杀（4） | -500 |
| 7 | 双方都没有 0~4 | 全盘加和，见 6.4 |

编号含义：

| 编号 | 名称 | 类别 |
|---:|---|---|
| 0 | 成五 | 单独最高档 |
| 1 | 活四 | 强必杀 |
| 2 | 双冲四 | 强必杀 |
| 3 | 三四 | 强必杀 |
| 4 | 双三 | 弱必杀 |

说明：

- 成五单独判定，不归入强必杀；
- 表中的成五、强必杀、弱必杀指的是僵持叶子上“存在这样的点”；实际落子成五仍由搜索终局叶子返回 ±1000；
- 对方强必杀 `-700`、弱必杀 `-500`，已经减去 150 先手优势；
- 这里的“必杀”是 18 子类点棋型的局部判定，不是 VCF/VCT 搜索证明；
- 文档不得把该判定表述为已经证明的必胜/必败。

## 6.4 第 7 档：双方都没有必杀

### 6.4.1 全盘加和

```text
W0 = 白表所有空点的 5~16 号基础分之和
B0 = 黑表所有空点的 5~16 号基础分之和 × 攻防系数
```

基础分表沿用现行两套。

白表（我方）：

| 组别 | 点型 | 分值 |
|---|---|---|
| 强威胁 | a1_bbb / a1_bbc / a1_bcc / a1_ccc | 400 / 350 / 300 / 250 |
| 强威胁 | a2_bbb / a2_bbc / a2_bcc / a2_ccc | 350 / 300 / 220 / 150 |
| 弱威胁 | b_bbb / b_bbc / b_bcc / b_ccc | 120 / 90 / 60 / 30 |

黑表（对方）：

| 组别 | 点型 | 分值 |
|---|---|---|
| 强威胁 | a1_bbb / a1_bbc / a1_bcc / a1_ccc | 250 / 200 / 160 / 120 |
| 强威胁 | a2_bbb / a2_bbc / a2_bcc / a2_ccc | 200 / 160 / 120 / 80 |
| 弱威胁 | b_bbb / b_bbc / b_bcc / b_ccc | 60 / 45 / 30 / 15 |

黑白使用两套表，用于体现先手优势：白方同型威胁的分值更高。

攻防系数使用白方当前档位，乘在黑方总和上：

| 白方档位 | 系数 |
|---|---:|
| 1 攻 4 防 | ×1.2 |
| 2 攻 3 防 | ×1.1 |
| 3 攻 2 防 | ×1.0 |
| 4 攻 1 防 | ×0.9 |

### 6.4.2 差后总分

```text
D = W0 - B0
```

### 6.4.3 平滑累进税

```text
叶子分 = 符号(D) × (0.05 × |D| + 475 × (1 - e^(−|D|/500)))
```

性质：

- 没有起征点，从 0 开始连续计税；
- 500 以下近似线性，税很轻；
- 500 以上边际价值逐渐降低，长期保留 5%；
- 有效税率逐渐接近 95%，但不会到 100%；
- 单调递增：原始差越大，税后一定越大，顺序不会反转；
- 没有硬上限，没有最终 clamp；
- 极端巨大的 `D` 可以超过 ±1000，表示局面已经接近必胜。

数值表（r = 0.05，S = 500）：

| 原始 D | 税后叶子分 | 有效税率 |
|---:|---:|---:|
| 0 | 0 | 0% |
| 100 | 91.1 | 8.9% |
| 500 | 325.3 | 34.9% |
| 1000 | 460.7 | 53.9% |
| 1500 | 526.4 | 64.9% |
| 2000 | 566.3 | 71.7% |
| 3500 | 649.6 | 81.4% |
| 5000 | 725.0 | 85.5% |
| 7500 | 850.0 | 88.7% |
| 8900 | 920.0 | 89.7% |
| 10500 | 1000.0 | 90.5% |

### 6.4.4 参数

```text
r = 0.05
S = 500
```

## 6.5 与旧实现的差异

| 维度 | 旧实现（代码事实） | 新设计（已实现） |
|---|---|---|
| 输入 | 最近一次双方上下文的候选子类快照 | 当前棋盘的白表、黑表 |
| 加和方式 | 前 5 名候选 × 名次权重 | 全盘 5~16 号直接求和 |
| 必杀处理 | 我方任意 0~4 取最大；对方 0~4 数量 ≥2 返回 -920 | 成五 / 强必杀 / 弱必杀分开，出现即判定 |
| 攻防档位 | 最后一步上下文的档位 | 白方当前档位 |
| 分数范围 | clamp 到 ±500 | 平滑累进税，无上限 |
| 与棋盘关系 | 历史快照，不是当前局面 | 当前实际局面 |

## 6.6 叶子分与五态的关系

- 叶子分只回答“局面分多少”，不设置五态；
- 必胜/必败仍由搜索树的五态聚合产生；
- 界面显示必须分别显示叶子评估模式和五态结果，不得互相冒充。

## 6.7 示例

假设当前白表有：

```text
a1_bbb × 2 = 800
b_bbb × 1 = 120
```

当前黑表有：

```text
a2_bbb × 1 = 200
b_bbc × 1 = 90
```

白方当前档位为 3 攻 2 防，攻防系数 ×1.0：

```text
W0 = 800 + 120 = 920
B0 = (200 + 90) × 1.0 = 290
D = 920 - 290 = 630
叶子分 = 0.05 × 630 + 475 × (1 - e^(−630/500))
       = 31.5 + 475 × (1 - e^(−1.26))
       ≈ 31.5 + 475 × 0.7163
       ≈ 371.7
```

## 6.8 实现约束

- 叶子时刻必须保证两张状态表与当前棋盘一致；状态表失步会直接导致叶子分错误；
- 扫描时只统计空点，占位格和 17 号按 0 处理；
- 攻防档位按根决策方当前局面实时计算；白方执子时即白方当前档位；
- 基础分表如再调整，必须同步本文档与回归测试。

---

# 第七章 五态强制胜负判定与向上汇总

## 7.1 状态编码

```text
+1  必胜       硬结论
+2  存在必胜   软证据
 0  无         没有发现证据
-2  存在必败   软证据
-1  必败       硬结论
```

所有状态均以**当前决策方视角**表示。

## 7.2 合并硬规则

```text
所有有效子节点都是 +1 -> 父节点 +1
所有有效子节点都是 -1 -> 父节点 -1
```

## 7.3 传播规则

其余情况下，只有硬状态 ±1 参与向上传播：

```text
任一子节点为 -1 -> 父节点 -2
否则任一子节点为 +1 -> 父节点 +2
否则 -> 0
```

结论：

```text
[-1, -1, -1] -> -1
[-1, -2, -1] -> -2
[-2, -2, -2] -> 0
[+1, -2]     -> +2
```

## 7.4 软证据不再向上传播

`+2` / `-2` 是当前决策层的局部软证据，只由硬状态 ±1 产生：

- 父层的 `-2` 只能来自子节点的 `-1`（硬必败），不能来自子节点的 `-2`；
- 父层的 `+2` 只能来自子节点的 `+1`（硬必胜），不能来自子节点的 `+2`；
- 子节点的 `+2` / `-2` 在父层聚合时按 `0` 处理。

因此：

```text
全是 -2 -> 0
全是 +2 -> 0
```

## 7.5 根节点状态

最终候选状态只取候选根节点的聚合结论。

深层某个子节点的状态不能直接当成根候选结论。

## 7.6 字段一致性

- `state`、`state_name`、`child_state`、`forced` 必须来自同一套五态判定；
- `score` 由叶子评估和加权回溯产生，不得混入状态分；
- `forced`、`fatal_ratio` 仅作兼容字段，不参与排序。

## 7.7 有限搜索边界

必胜/必败只针对本次实际展开且未被剪掉的搜索树。

代码、界面和文档不得表述为全盘穷举的数学证明。

## 7.8 界面提示

当所有候选都是 `-2` 时，界面显示：

```text
⚠ 全部候选均发现败势分支，局面高危
```

这是风险提示，不是硬必败结论。

---

# 第八章 分支分回溯与候选点分数汇总

## 8.1 叶子分到分支分

`_weighted_average`：

```python
sum(w * value for w, value in items)
```

不归一化。

含义：

- 根层以下：路径权重 = `oq1 × q2 × oq2 × q3 × oq3 × ...`；
- 根节点自身的候选质量不计入路径权重；
- 根层只看对方 `oq`，中间层双方 `q` / `oq` 都乘；
- 深层权重自然衰减；
- 无需除以 ΣW。

## 8.2 根候选分数

每个根候选只按对方应手质量汇总：

```text
根候选分 = Σ(对方应手质量 oq × 子树分数)
```

其中：

- `oq`：对方该应手的质量；
- `子树分数`：递归返回的分支分或叶子分；
- 根候选自己的权重 `w_root` 不进入此公式，也不进入路径权重。

根候选权重的作用仅限：

- 通过耗温公式决定该分支的初始温度消耗，从而决定推演深度；
- 作为上下文里的 `move_w` 记录，供实验性神经网络路径使用。

根层与中间层的区别：

| 层级 | 当前方自己的质量 `q` |
|---|---|
| 根层 | 不参与分数、路径权重和剪枝 |
| 中间层 | 参与路径权重、剪枝和分数汇总 |

所以两个根候选即使权重不同，如果对方应手和子树叶子完全相同，根候选分也相同。

## 8.3 候选排序

排序键：

```python
_pick_key_state(state, score) = (STATE_ORDER[state], score)
```

顺序：

```text
必胜 > 存在必胜 > 无 > 存在必败 > 必败
```

同档按 `score` 降序。

## 8.4 分数与状态分离

- `score`：叶子评估 + 路径加权回溯；
- `state`：五态强制胜负判定；
- 两者不能混用。

## 8.5 字段使用现状（第六章确认后的已实现口径）

| 字段 | 对候选分的影响 |
|---|---|
| 当前棋盘白表 / 黑表 | 直接决定叶子分，见第六章 |
| `gear` | 第 7 档时使用白方当前档位，乘在黑方总和上 |
| 根候选权重 `w_root` | 只通过耗温影响推演深度；不进入根候选分、路径权重或剪枝 |
| 中间层候选权重 `w` | 通过 `_dt`、`_step_quality`、剪枝、路径权重影响分数 |
| `move_w` | 不直接进入叶子分 |
| `cand_ws` | 不直接进入叶子分 |
| `cand_sub_ids` | 不再作为叶子分输入 |
| `kill` | 不再直接进入叶子分；必杀由当前状态表判定 |
| `temp` | 只决定何时成为叶子 |
| `depth` | 不参与分数 |
| `fatal` | 用于五态，不参与叶子分 |

> 当前代码已按第六章口径实现；上表为实际字段使用。

## 8.6 结论

- 分支分公式保持“路径权重 × 子分数”的加权汇总不变；
- 叶子分来源为当前局面的全盘加和；
- 根候选权重只控制推演深度，不进入根候选分、路径权重或剪枝；
- 中间层候选权重通过温度、剪枝和路径权重影响最终分；
- 五态与分数仍然分离。

---

# 第九章 最终落子选择

## 9.1 直取分支

`Engine.analyze_turn` 中：

```text
my-five / opp-five -> 直接落子，不深推
唯一候选           -> 直接落子，不深推
```

## 9.2 深推分支

非直取情况下：

```text
rank_candidates 对全部候选深推打分、排序
move = ranked[0]
```

## 9.3 兜底规则

- 只要存在非必败候选，不选必败候选；
- 若所有候选都是败势档，按状态顺序和分数选兜底着法；
- 保证程序能够继续落子。

## 9.4 决策网络（可选，默认关闭）

`Engine._decision_pick`：

- `use_decision_net = False` 时返回纯算法第一名；
- 网络启用且可用时可能覆盖 `move`；
- 网络异常时回退纯算法第一名。

## 9.5 界面显示

- 候选状态来自本次实际 `ranked`；
- 叶子评估显示“纯算法（N 个叶子）”或“叶子未触发”；
- 只有真实完成网络推理时才显示神经网络。

---

# 附录 A 测试与验收（建议保留）

- [ ] 单方向 10 类父类判定回归（当前由 `test_pattern_level_fast.py` 的等价性测试间接覆盖）。
- [ ] 18 子类点棋型回归（同上）。
- [ ] 候选生成：a1 单冲四、活四近端、opp-vcf 下己方先手点。
- [x] 叶子评估：全盘加和、攻防系数、平滑累进税回归。（`test_leaf_value.py`）
- [x] 叶子评估：成五 / 强必杀 / 弱必杀优先级，黑白双方都要覆盖。（`test_leaf_value.py`）
- [x] 叶子评估：极端 D 不截断的行为测试。（`test_leaf_value.py`）
- [x] 状态表与棋盘一致性：增量刷新与 `rebuild_all` 逐格一致。（`test_state_exact.py`）
- [x] 五态聚合测试：`[-1,-2,-1] -> -2` 等。（`test_child_state.py`）
- [x] 单点求值档位路径与记录式路径等价。（`test_pattern_level_fast.py`）
- [ ] 黑白对称测试：状态表对称已由
      `test_state_exact.py::test_color_swap_tables_are_mirrored` 覆盖；
      整条决策链的黑白对称仍无专测。
- [ ] 完整 Python 回归：

```bash
python -m unittest discover -s gomoku/tests -v
```

- [ ] T0=80 / 160 / 240 性能对比。
- [ ] 本机 `games.json` 中 `local` 这局棋谱复现。
- [ ] GUI 人工检查：候选显示、叶子数、五态标签。

---

# 附录 B 待确认问题汇总

1. 候选生成漏点是否与叶子评估一起修？
2. 巨大威胁是否需要 VCF/VCT 搜索来确认，而不是只靠静态加和？
3. 性能预算：每个叶子扫描两张 15×15 表并计算指数函数的接受上限？
4. 验收用的最小复现局面和期望输出。
