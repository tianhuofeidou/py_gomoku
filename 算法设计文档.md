# 算法设计文档

> 状态：第五章路径权重/剪枝、第六章叶子评估、第七章五态聚合和第八章汇总口径已按用户确认更新；其余章节仍可能调整。
>
> 本文档区分三类内容：
>
> - **代码事实**：当前实现确实如此；
> - **已知问题**：当前实现存在的问题或缺口；
> - **待确认**：需要用户最终拍板的设计点，未确认前不得按文档直接改代码。

---

# 第一章 总览

## 1.1 目的与范围

本文档描述五子棋 AI 的完整算法链：

```text
棋型识别
  -> 候选点选择
  -> 深度推演
  -> 叶子评估
  -> 强制胜负判定
  -> 候选点分数汇总
  -> 最终落子选择
```

覆盖模块：

```text
pattern -> search -> deep_search -> engine
```

不覆盖：

- 棋局规则、会话、GUI、记忆、插件适配层；
- 对局存档格式；
- 神经网络训练流程（只描述接口和输入）。

## 1.2 总体决策链

```text
Engine.analyze_turn
  ├─ Search.tactical_candidates   战术优先级，命中即止
  ├─ Search._fallback             普通候选，3攻2防
  └─ DeepSearch.rank_candidates   对候选逐个深推打分、排序
        └─ 返回 part4_result.move / ranked
```

## 1.3 核心术语

| 术语 | 含义 |
|---|---|
| 父类线棋型 | 单方向扫描得到的 10 类线棋型 |
| 18 子类 | 四个方向组合后每个空位的 18 种点棋型编号 |
| 候选点 | 准备进入搜索或直接选择的落子位置 |
| 根候选 | `rank_candidates` 的顶层候选 |
| 分支 | 从根候选开始的一串模拟落子 |
| 叶子 | 温度耗尽、成五或没有候选时的终止节点 |
| 路径权重 | `path_w`，候选步质量的连乘衰减 |
| 五态 | `+1/+2/0/-2/-1` 强制胜负状态 |
| 叶子分 | 叶子节点的评估分数 |
| 分支分 | 叶子分按路径权重回溯得到的根候选分数 |

## 1.4 模块职责

| 模块 | 职责 |
|---|---|
| `pattern.py` | 单方向线棋型、四方向点棋型、18 子类基础能力 |
| `search.py` | 黑白状态表、局部更新、候选生成、攻防档位 |
| `deep_search.py` | 温度银行、递归、剪枝、叶子评估、五态判定 |
| `engine.py` | 组织候选、调用深推、输出最终选点 |

## 1.5 文档标记约定

- **代码事实**：与当前代码一致。
- **已知问题**：当前代码的缺口，后续可能修改。
- **待确认**：需要用户确认的算法设计决策。

---

# 第二章 单方向线棋型的 10 类父类判定

## 2.1 扫描原语：`_scan_two`

`pattern._scan_two(board, r, c, player, dr, dc)` 从基子出发，沿一个方向向两侧扫描。

扫描产出：

| 字段 | 含义 |
|---|---|
| `stones` | 连续段内计入的己方子数 |
| `l_gap` / `r_gap` | 两侧缝数量 |
| `l_gap_locations` / `r_gap_locations` | 缝到基子的距离 |
| `l_edge` / `r_edge` | 端点和端类型 |

端类型：

| 类型 | 含义 |
|---|---|
| 1 | 活端 `__` 或 `_x` |
| 2 | 堵端 `|` |
| 3 | 半活端 `_|` |
| 4 | 断裂端（第二个 `_x`） |
| 5 | 延伸端 `__x` |

## 2.2 单方向父类：10 类

`pattern._classify` 输出以下 10 类之一：

```text
FIVE
LIVE4
SLEEP4
LIVE3
SLEEP3
LIVE2
SLEEP2
DEAD
SINGLE
NONE
```

判定优先级（简化）：

| 父类 | 条件 |
|---|---|
| `DEAD` | 两端都是堵/半活，且段长 < 5 |
| `SINGLE` | `stones == 1` |
| `FIVE` | `stones >= 5` 且无 gap |
| `LIVE4` | 4 子连续，两端都是活端 |
| `SLEEP4` | 4 子非活四，或 5 子以上带缝 |
| `LIVE3` | 3 子且满足活三端条件 |
| `SLEEP3` | 3 子但非活三 |
| `LIVE2` | 2 子且满足活二端条件 |
| `SLEEP2` | 2 子但非活二 |
| `NONE` | 其余 |

## 2.3 特殊处理

- `_overall_judge`：双缝 `x_X_x` 的跳三整体判定为眠三；
- `_normalize`：左右各一缝时拆成两条记录；
- `_synth_double_gap`：处理双空跳 `x__x` 的二连级判定；
- 4 连截断：避免把活四误判成带缝眠四。

## 2.4 与档位的关系

`search.DIR_LEVEL` 把父类映射为 0～5 档：

```text
FIVE   -> 0
LIVE4  -> 1
SLEEP4 -> 2
LIVE3  -> 3
SLEEP3 -> 4
LIVE2  -> 4
SLEEP2 -> 5
DEAD   -> 5
SINGLE -> 5
NONE   -> 5
```

## 2.5 热路径档位查询（`Search._direction_level`）

`Search._eval` 是全引擎最热的函数：一次深推决策约调用 6 万次，每次都要回答
「该点在四个方向上分别形成哪一档」。它走**档位路径**，不走记录式路径：

| 路径 | 产出 | 用途 |
|---|---|---|
| `analyze_direction` / `analyze_point` | 完整棋型记录（含子类名、`gap_sides`） | 对外接口、防守反推、候选签名 |
| `Search._direction_level` | 仅该方向的档位 0～5 | `_eval` 专用 |

两条路径对档位的影响必须严格一致：状态表编号直接决定候选分、攻防档位和叶子评估，
一旦不一致就会静默改变选点与评分。

等价性依据：

1. `parent` 是 `(stones, gap_location, l_edge, r_edge)` 的纯函数（`_classify`）。
   档位路径省略的 `sub_id`、`sub_name`、`gap_sides` 都不在它的输入集里
   （`gap_sides` 仅供 `_subclass` 区分边跳/中跳子类）。
2. 落子点在某个方向的**两侧 3 格内都没有己方子**时，该方向必为档位 5，可直接返回。
   依据：`_scan_two` 遇「连续 3 空 / 对方子 / 边界」即停，且 `stones` 初值为 1；
   某侧 3 格内无己方子时该侧必在 `i<=3` 内结束、`stones` 保持 1，
   而 `_classify` 对 `stones==1` 只返回 `SINGLE` 或 `DEAD`，两者档位都是 5；
   同时该侧端类型不可能是 5，端 5 缝合分支也不会触发。
   - 必须查**两侧**：`_scan_two` 是双向扫描（只查单侧会在随机局面上错约 5%）；
   - 必须查到**第 3 格**：双空前瞻会读距离 3，那里有己方子会合成 `LIVE2`（档位 4）。
3. 端 5 缝合（`_synth_double_gap`）不能省：它把 `SINGLE`（档位 5）换成
   `LIVE2`（档位 4），而档位 4 在 `_classify_levels` 里要计入 B 的数量。
   合成记录可能为空，此时档位保持 5。

档位路径不改变任何判定，只省略不影响档位的计算。本机实测（两个中盘深推局面，
`T0=160`）：单点求值 20.5µs → 6.9µs，单步决策 3.47s → 2.20s，叶子数与选点不变。

回归：`gomoku/tests/test_pattern_level_fast.py`。

---

# 第三章 四方向组合的 18 子类点棋型判定

## 3.1 `analyze_point`

对空位 `(r, c)` 模拟落子后，沿四个方向调用 `analyze_direction`，汇总得到多条父类记录。

## 3.2 四方向档位

每个方向取最高档位，得到 4 个档位值 `[lv0, lv1, lv2, lv3]`。

## 3.3 `_classify_levels` 组合规则

设：

```text
n0 = 档位为 0（FIVE）的方向数
n1 = 档位为 1（LIVE4）的方向数
n2 = 档位为 2（SLEEP4）的方向数
n3 = 档位为 3（LIVE3）的方向数
```

判定顺序：

```text
n0 >= 1            -> five    (0)
n1 >= 1            -> live4   (1)
n2 >= 2            -> d44     (2)
n2 >= 1 且 n3 >= 1 -> d34     (3)
n3 >= 2            -> d33     (4)
恰好 1 个眠四       -> a1 系   (5-8)
恰好 1 个活三       -> a2 系   (9-12)
否则按 B 的数量      -> b 系    (13-16) / none (17)
```

## 3.4 18 子类表

| 编号 | 名称 | 父类家族 |
|---:|---|---|
| 0 | `five` | KILL |
| 1 | `live4` | KILL |
| 2 | `d44` | KILL |
| 3 | `d34` | KILL |
| 4 | `d33` | KILL |
| 5 | `a1_bbb` | THREAT |
| 6 | `a1_bbc` | THREAT |
| 7 | `a1_bcc` | THREAT |
| 8 | `a1_ccc` | THREAT |
| 9 | `a2_bbb` | THREAT |
| 10 | `a2_bbc` | THREAT |
| 11 | `a2_bcc` | THREAT |
| 12 | `a2_ccc` | THREAT |
| 13 | `b_bbb` | POTEN |
| 14 | `b_bbc` | POTEN |
| 15 | `b_bcc` | POTEN |
| 16 | `b_ccc` | POTEN |
| 17 | `none` | USELESS |

## 3.5 18 → 4 父类家族

```text
KILL     : 0-4
THREAT   : 5-12
POTEN    : 13-16
USELESS  : 17
```

## 3.6 状态表

`Search.sb`、`Search.sw` 分别保存黑白双方每个空位“如果在此落子”的 18 子类编号。

- 真实落子和模拟落子后做局部增量更新，范围就是**棋段区**：
  落子点所在棋段（close / 连续三空为界的段内全部空缝）+ 段**两端**外侧尾部空位**全取**。
  `_scan_side` 遇连续三空即停，所以尾部最多 3 格，"全取"即 ≤3 格。
- **该范围是增量更新的唯一来源。** 把两端尾部取满之后，增量结果与 `rebuild_all`
  逐格一致（实测单步漂移 0、24 局累计漂移 0）。因此：
  - 历史上的"定点区"（切比雪夫半径 2 邻域）已删除——它覆盖的格子里，
    真正会被本手影响的都在棋段区两端尾部内，其余 8 格是"马步"位置，与落子点不共线；
  - 历史上的"状态区"（按 KILL/THREAT 筛格、空则降级 POTEN）已删除——范围完备后
    它只剩重复劳动，去掉后两张表逐格不变，而深推决策耗时约减半；
  - 历史上的"段1/段2/段3"按落子方分叉的调度已删除，黑白两条路径完全同构。
- 深推模拟使用 journal 成对回滚；
- 悔棋或重建棋局使用 `rebuild_all` 全量重算。
- 为什么必须取到尾部第 3 格：`_scan_two` 的双空前瞻最多往落子点方向看 2 格，
  于是"会被本手影响、却落在尾部第 3 格"的格子有两类——
  `● _ _ □`（□ 的双空跳搭档就是本手）与 `□ _ _ x ●`（□ 的搭档是 x，
  本手贴着搭档外侧，改的是搭档的外侧端口）。取 2 会漏掉这两类。

---

# 第四章 候选点选择

## 4.1 `Search.candidates`

当前按检测优先级分类（命中即止只看 0 级必杀）：

```text
w_five : 白方成五点
b_five : 黑方成五点
w_vcf  : 白方活四 / 双冲四 / 三四
b_vcf  : 黑方活四 / 双冲四 / 三四
w_d33  : 白方双活三
b_d33  : 黑方双活三
```

对应子类：

```text
five   : (0,)
vcf    : (1, 2, 3)
d33    : (4,)
```

## 4.2 `Search.tactical_candidates`

检测顺序：

```text
my-five
opp-five
my-vcf
opp-vcf
my-d33
opp-d33
```

返回：

- `my-*`：`{点: 100}`；
- `opp-five`：`{点: 100}`；
- `opp-vcf` / `opp-d33`：`_defense_candidates` 生成的防守点，权重 100 / 80 / 60。

## 4.3 `_defense_candidates`

对每个对方威胁点：

1. 模拟对方在该点落子；
2. 中心点权重 100（is_kill）或 60；
3. 对各条 LIV4 / SLEEP4 / LIVE3 记录提取端点；
4. 提取两子之间的缝位；
5. 返回 `{(r,c): 权重}`。

## 4.4 `Search._fallback`

默认 3 攻 2 防。

候选池：

```text
状态父类为 THREAT 或 POTEN 的空位
```

排序：

```text
_candidate_score = (
    TYPE_SCORE[subclass]
    + (neighbor_score / NEIGHBOR_NORM) * W_NEIGHBOR
    + dual_bonus
) / SCORE_MAX * 100
```

去重：

- 局面有对称性：对称轨道；
- 否则：棋型签名。

记忆修正：

- 胜谱落点加分；
- 败谱落点减分；
- 上限 ±MEM_ADJ_MAX。

## 4.5 攻防档位

`_choose_gear` / `_target_gear` 根据双方 A1/A2/B 压力和历史 0 级动量选择 1～4 档：

```text
1攻4防 / 2攻3防 / 3攻2防 / 4攻1防
```

## 4.6 已知问题

**已知问题 4.6.1**
`candidates()` 的 `vcf` 只包含子类 1/2/3，a1 单冲四（子类 5-8）不进入 `w_vcf`。

**已知问题 4.6.2**
`_defense_candidates` 对 `d <= 1` 的近端空位直接跳过，导致活四近端防守点缺失。

**已知问题 4.6.3**
`opp-vcf` 分支只返回对方威胁的防守点，不保留当前方自己的 a1 先手点。

**已知问题 4.6.4**
`_fallback` 按静态 `_candidate_score` 取前 3 攻 2 防，可能漏掉静态分低但深推是胜着的点。

## 4.7 待确认

- 候选点选择是否按“所有威胁”重新设计？
- 防守点生成是否要覆盖 a1/a2 的全部强制点？
- 己方先手点和对方防守点的优先级如何合并？
- 候选权重必须保留原始语义，不能静默转成等权列表。

---

# 第五章 深度推演（温度银行与权重剪枝法）

## 5.1 候选展开

`_candidate_points(board, player, n=5, plies=0)`：

1. 先调用 `tactical_candidates`；
2. 命中则取前 n 个候选，保留权重；
3. 未命中则调用 `_fallback`，取前 n 个，权重 clamp 到 `[1, 100]`。

深度收窄：

```text
回合数 rounds = plies // 2
rounds >= NARROW_ROUNDS      -> 最多 CAND_N_MID 个
rounds >= HARD_NARROW_ROUNDS -> 最多 CAND_N_DEEP 个
```

当前默认：

```text
CAND_N_FULL = 5
CAND_N_MID  = 3
CAND_N_DEEP = 1
NARROW_ROUNDS = 4
HARD_NARROW_ROUNDS = 8
```

## 5.2 温度银行

单步耗温：

```text
_dt(w) = DT_M * ((100 - w) / 100) ^ DT_POW + DT_N
```

当前参数：

```text
T0      = 160
DT_M    = 400
DT_POW  = 2
DT_N    = 3
```

一步完整回合：

```text
temp2 = temp - _dt(w) - _dt(ow)
```

其中：

- `w`：当前方落子权重；
- `ow`：对方应手权重。

## 5.3 路径权重

每一步只在本方候选列表内归一化：

```text
我方这一步：q  = 我方候选权重 / 我方本层最高候选权重
对方应手：  oq = 对方应手权重 / 对方本层最高候选权重
```

根层以下，双方都会乘进路径权重：

```text
路径权重 = oq1 × q2 × oq2 × q3 × oq3 × ...
```

其中 `oq1` 是根候选之后对方第一次应手的质量，`q2` 是下一轮我方候选的质量。

根候选自己的 `q_root` 不进入路径权重。

特点：

- 单步质量只在本方候选列表内比较，不把双方候选合在一起取最大；
- 根层只看对方 `oq`，根层以下双方 `q` / `oq` 都乘；
- 全程走最优候选时路径权重约为 1；
- 走弱分支贡献按权重缩水；
- 深层连乘自然衰减。

## 5.4 剪枝

`_prune_branches` 按绝对路径权重剪枝：

- 根层：对方应手分支按 `oq` 剪枝；
- 中间层：按 `path_w × q` 剪枝，对方应手层再乘 `oq`；
- 绝对路径权重低于 `PRUNE_W = 0.02` 的分支剔除；
- 至少保留权重最大的一支，防止整层被剪空。

## 5.5 `_recursive` 流程

```text
1. 当前方生成候选
2. 按路径权重剪枝
3. 对每个保留候选：
   a. 构造 ctx
   b. 模拟落子
   c. 同步更新状态表（journal）
   d. 成五 -> 叶子
   e. 否则生成对方候选
   f. 温度耗尽 -> 叶子
   g. 否则递归
   h. 回滚 journal、棋盘、branch、ctx_branch
4. 加权汇总本层分数
5. 合并五态状态
6. 写入 cache
```

## 5.6 cache

cache key：

```text
tuple((r, c) for (r, c, player, w) in branch)
```

含义：同一个落子序列只推一次，前缀可复用。

---

# 第六章 叶子评估

> 本章为已确认并已实现的叶子评估口径；实现位于 `gomoku/core/deep_search.py`，回归位于 `gomoku/tests/test_leaf_value.py`。

## 6.1 叶子触发时机

推演中某一步直接成五时，叶子状态为 `win` / `lose`，返回实际终局分 `±1000`。

温度耗尽或对方无应手时，叶子状态为 `stale`，进入本章的僵持叶子评估。

僵持叶子评估的对象是温度耗尽时的当前棋盘，即黑刚应完、轮到白走。

## 6.2 数据来源

不看历史上下文快照，直接读当前棋盘的两张状态表：

| 表 | 内容 |
|---|---|
| 白表 | 每个空点白若落子会形成的 18 子类编号 |
| 黑表 | 每个空点黑若落子会形成的 18 子类编号 |

占位格和 17 号都按 0 分处理。

> 代码按“根决策方 / 对手”对称实现：根决策方是白时，白表即根方表；根决策方是黑时角色对调，分值仍按“根方用根方基础分表、对手用对手基础分表”处理。

## 6.3 必杀判定优先级

从上往下查，命中即停。成五、强必杀、弱必杀都按“有 / 没有”判定，不数个数。

| 优先级 | 条件 | 叶子分 |
|---:|---|---:|
| 1 | 白表出现成五（0） | +920 |
| 2 | 黑表出现成五（0） | -920 |
| 3 | 白表出现强必杀（1/2/3） | +850 |
| 4 | 黑表出现强必杀（1/2/3） | -700 |
| 5 | 白表出现弱必杀（4） | +650 |
| 6 | 黑表出现弱必杀（4） | -500 |
| 7 | 双方都没有 0~4 | 全盘加和，见 6.4 |

编号含义：

| 编号 | 名称 | 类别 |
|---:|---|---|
| 0 | 成五 | 单独最高档 |
| 1 | 活四 | 强必杀 |
| 2 | 双冲四 | 强必杀 |
| 3 | 三四 | 强必杀 |
| 4 | 双三 | 弱必杀 |

说明：

- 成五单独判定，不归入强必杀；
- 表中的成五、强必杀、弱必杀指的是僵持叶子上“存在这样的点”；实际落子成五仍由搜索终局叶子返回 ±1000；
- 对方强必杀 `-700`、弱必杀 `-500`，已经减去 150 先手优势；
- 这里的“必杀”是 18 子类点棋型的局部判定，不是 VCF/VCT 搜索证明；
- 文档不得把该判定表述为已经证明的必胜/必败。

## 6.4 第 7 档：双方都没有必杀

### 6.4.1 全盘加和

```text
W0 = 白表所有空点的 5~16 号基础分之和
B0 = 黑表所有空点的 5~16 号基础分之和 × 攻防系数
```

基础分表沿用现行两套。

白表（我方）：

| 组别 | 点型 | 分值 |
|---|---|---|
| 强威胁 | a1_bbb / a1_bbc / a1_bcc / a1_ccc | 400 / 350 / 300 / 250 |
| 强威胁 | a2_bbb / a2_bbc / a2_bcc / a2_ccc | 350 / 300 / 220 / 150 |
| 弱威胁 | b_bbb / b_bbc / b_bcc / b_ccc | 120 / 90 / 60 / 30 |

黑表（对方）：

| 组别 | 点型 | 分值 |
|---|---|---|
| 强威胁 | a1_bbb / a1_bbc / a1_bcc / a1_ccc | 250 / 200 / 160 / 120 |
| 强威胁 | a2_bbb / a2_bbc / a2_bcc / a2_ccc | 200 / 160 / 120 / 80 |
| 弱威胁 | b_bbb / b_bbc / b_bcc / b_ccc | 60 / 45 / 30 / 15 |

黑白使用两套表，用于体现先手优势：白方同型威胁的分值更高。

攻防系数使用白方当前档位，乘在黑方总和上：

| 白方档位 | 系数 |
|---|---:|
| 1 攻 4 防 | ×1.2 |
| 2 攻 3 防 | ×1.1 |
| 3 攻 2 防 | ×1.0 |
| 4 攻 1 防 | ×0.9 |

### 6.4.2 差后总分

```text
D = W0 - B0
```

### 6.4.3 平滑累进税

```text
叶子分 = 符号(D) × (0.05 × |D| + 475 × (1 - e^(−|D|/500)))
```

性质：

- 没有起征点，从 0 开始连续计税；
- 500 以下近似线性，税很轻；
- 500 以上边际价值逐渐降低，长期保留 5%；
- 有效税率逐渐接近 95%，但不会到 100%；
- 单调递增：原始差越大，税后一定越大，顺序不会反转；
- 没有硬上限，没有最终 clamp；
- 极端巨大的 `D` 可以超过 ±1000，表示局面已经接近必胜。

数值表（r = 0.05，S = 500）：

| 原始 D | 税后叶子分 | 有效税率 |
|---:|---:|---:|
| 0 | 0 | 0% |
| 100 | 91.1 | 8.9% |
| 500 | 325.3 | 34.9% |
| 1000 | 460.7 | 53.9% |
| 1500 | 526.4 | 64.9% |
| 2000 | 566.3 | 71.7% |
| 3500 | 649.6 | 81.4% |
| 5000 | 725.0 | 85.5% |
| 7500 | 850.0 | 88.7% |
| 8900 | 920.0 | 89.7% |
| 10500 | 1000.0 | 90.5% |

### 6.4.4 参数

```text
r = 0.05
S = 500
```

## 6.5 与旧实现的差异

| 维度 | 旧实现（代码事实） | 新设计（已实现） |
|---|---|---|
| 输入 | 最近一次双方上下文的候选子类快照 | 当前棋盘的白表、黑表 |
| 加和方式 | 前 5 名候选 × 名次权重 | 全盘 5~16 号直接求和 |
| 必杀处理 | 我方任意 0~4 取最大；对方 0~4 数量 ≥2 返回 -920 | 成五 / 强必杀 / 弱必杀分开，出现即判定 |
| 攻防档位 | 最后一步上下文的档位 | 白方当前档位 |
| 分数范围 | clamp 到 ±500 | 平滑累进税，无上限 |
| 与棋盘关系 | 历史快照，不是当前局面 | 当前实际局面 |

## 6.6 叶子分与五态的关系

- 叶子分只回答“局面分多少”，不设置五态；
- 必胜/必败仍由搜索树的五态聚合产生；
- 界面显示必须分别显示叶子评估模式和五态结果，不得互相冒充。

## 6.7 示例

假设当前白表有：

```text
a1_bbb × 2 = 800
b_bbb × 1 = 120
```

当前黑表有：

```text
a2_bbb × 1 = 200
b_bbc × 1 = 90
```

白方当前档位为 3 攻 2 防，攻防系数 ×1.0：

```text
W0 = 800 + 120 = 920
B0 = (200 + 90) × 1.0 = 290
D = 920 - 290 = 630
叶子分 = 0.05 × 630 + 475 × (1 - e^(−630/500))
       = 31.5 + 475 × (1 - e^(−1.26))
       ≈ 31.5 + 475 × 0.7163
       ≈ 371.7
```

## 6.8 实现约束

- 叶子时刻必须保证两张状态表与当前棋盘一致；状态表失步会直接导致叶子分错误；
- 扫描时只统计空点，占位格和 17 号按 0 处理；
- 攻防档位按根决策方当前局面实时计算；白方执子时即白方当前档位；
- 基础分表如再调整，必须同步本文档与回归测试。

---

# 第七章 五态强制胜负判定与向上汇总

## 7.1 状态编码

```text
+1  必胜       硬结论
+2  存在必胜   软证据
 0  无         没有发现证据
-2  存在必败   软证据
-1  必败       硬结论
```

所有状态均以**当前决策方视角**表示。

## 7.2 合并硬规则

```text
所有有效子节点都是 +1 -> 父节点 +1
所有有效子节点都是 -1 -> 父节点 -1
```

## 7.3 传播规则

其余情况下，只有硬状态 ±1 参与向上传播：

```text
任一子节点为 -1 -> 父节点 -2
否则任一子节点为 +1 -> 父节点 +2
否则 -> 0
```

结论：

```text
[-1, -1, -1] -> -1
[-1, -2, -1] -> -2
[-2, -2, -2] -> 0
[+1, -2]     -> +2
```

## 7.4 软证据不再向上传播

`+2` / `-2` 是当前决策层的局部软证据，只由硬状态 ±1 产生：

- 父层的 `-2` 只能来自子节点的 `-1`（硬必败），不能来自子节点的 `-2`；
- 父层的 `+2` 只能来自子节点的 `+1`（硬必胜），不能来自子节点的 `+2`；
- 子节点的 `+2` / `-2` 在父层聚合时按 `0` 处理。

因此：

```text
全是 -2 -> 0
全是 +2 -> 0
```

## 7.5 根节点状态

最终候选状态只取候选根节点的聚合结论。

深层某个子节点的状态不能直接当成根候选结论。

## 7.6 字段一致性

- `state`、`state_name`、`child_state`、`forced` 必须来自同一套五态判定；
- `score` 由叶子评估和加权回溯产生，不得混入状态分；
- `forced`、`fatal_ratio` 仅作兼容字段，不参与排序。

## 7.7 有限搜索边界

必胜/必败只针对本次实际展开且未被剪掉的搜索树。

代码、界面和文档不得表述为全盘穷举的数学证明。

## 7.8 界面提示

当所有候选都是 `-2` 时，界面显示：

```text
⚠ 全部候选均发现败势分支，局面高危
```

这是风险提示，不是硬必败结论。

---

# 第八章 分支分回溯与候选点分数汇总

## 8.1 叶子分到分支分

`_weighted_average`：

```python
sum(w * value for w, value in items)
```

不归一化。

含义：

- 根层以下：路径权重 = `oq1 × q2 × oq2 × q3 × oq3 × ...`；
- 根节点自身的候选质量不计入路径权重；
- 根层只看对方 `oq`，中间层双方 `q` / `oq` 都乘；
- 深层权重自然衰减；
- 无需除以 ΣW。

## 8.2 根候选分数

每个根候选只按对方应手质量汇总：

```text
根候选分 = Σ(对方应手质量 oq × 子树分数)
```

其中：

- `oq`：对方该应手的质量；
- `子树分数`：递归返回的分支分或叶子分；
- 根候选自己的权重 `w_root` 不进入此公式，也不进入路径权重。

根候选权重的作用仅限：

- 通过耗温公式决定该分支的初始温度消耗，从而决定推演深度；
- 作为上下文里的 `move_w` 记录，供实验性神经网络路径使用。

根层与中间层的区别：

| 层级 | 当前方自己的质量 `q` |
|---|---|
| 根层 | 不参与分数、路径权重和剪枝 |
| 中间层 | 参与路径权重、剪枝和分数汇总 |

所以两个根候选即使权重不同，如果对方应手和子树叶子完全相同，根候选分也相同。

## 8.3 候选排序

排序键：

```python
_pick_key_state(state, score) = (STATE_ORDER[state], score)
```

顺序：

```text
必胜 > 存在必胜 > 无 > 存在必败 > 必败
```

同档按 `score` 降序。

## 8.4 分数与状态分离

- `score`：叶子评估 + 路径加权回溯；
- `state`：五态强制胜负判定；
- 两者不能混用。

## 8.5 字段使用现状（第六章确认后的已实现口径）

| 字段 | 对候选分的影响 |
|---|---|
| 当前棋盘白表 / 黑表 | 直接决定叶子分，见第六章 |
| `gear` | 第 7 档时使用白方当前档位，乘在黑方总和上 |
| 根候选权重 `w_root` | 只通过耗温影响推演深度；不进入根候选分、路径权重或剪枝 |
| 中间层候选权重 `w` | 通过 `_dt`、`_step_quality`、剪枝、路径权重影响分数 |
| `move_w` | 不直接进入叶子分 |
| `cand_ws` | 不直接进入叶子分 |
| `cand_sub_ids` | 不再作为叶子分输入 |
| `kill` | 不再直接进入叶子分；必杀由当前状态表判定 |
| `temp` | 只决定何时成为叶子 |
| `depth` | 不参与分数 |
| `fatal` | 用于五态，不参与叶子分 |

> 当前代码已按第六章口径实现；上表为实际字段使用。

## 8.6 结论

- 分支分公式保持“路径权重 × 子分数”的加权汇总不变；
- 叶子分来源为当前局面的全盘加和；
- 根候选权重只控制推演深度，不进入根候选分、路径权重或剪枝；
- 中间层候选权重通过温度、剪枝和路径权重影响最终分；
- 五态与分数仍然分离。

---

# 第九章 最终落子选择

## 9.1 直取分支

`Engine.analyze_turn` 中：

```text
my-five / opp-five -> 直接落子，不深推
唯一候选           -> 直接落子，不深推
```

## 9.2 深推分支

非直取情况下：

```text
rank_candidates 对全部候选深推打分、排序
move = ranked[0]
```

## 9.3 兜底规则

- 只要存在非必败候选，不选必败候选；
- 若所有候选都是败势档，按状态顺序和分数选兜底着法；
- 保证程序能够继续落子。

## 9.4 决策网络（可选，默认关闭）

`Engine._decision_pick`：

- `use_decision_net = False` 时返回纯算法第一名；
- 网络启用且可用时可能覆盖 `move`；
- 网络异常时回退纯算法第一名。

## 9.5 界面显示

- 候选状态来自本次实际 `ranked`；
- 叶子评估显示“纯算法（N 个叶子）”或“叶子未触发”；
- 只有真实完成网络推理时才显示神经网络。

---

# 附录 A 测试与验收（建议保留）

- [ ] 单方向 10 类父类判定回归。
- [ ] 18 子类点棋型回归。
- [ ] 候选生成：a1 单冲四、活四近端、opp-vcf 下己方先手点。
- [x] 叶子评估：全盘加和、攻防系数、平滑累进税回归。
- [x] 叶子评估：成五 / 强必杀 / 弱必杀优先级，黑白双方都要覆盖。
- [x] 叶子评估：极端 D 不截断的行为测试。
- [ ] 叶子时刻状态表与棋盘一致性测试。
- [ ] 五态聚合测试：`[-1,-2,-1] -> -2` 等。
- [ ] 黑白对称测试。
- [ ] 完整 Python 回归：

```bash
python -m unittest discover -s gomoku/tests -v
```

- [ ] T0=80 / 160 / 240 性能对比。
- [ ] 本机 `games.json` 中 `local` 这局棋谱复现。
- [ ] GUI 人工检查：候选显示、叶子数、五态标签。

---

# 附录 B 待确认问题汇总

1. 候选生成漏点是否与叶子评估一起修？
2. 巨大威胁是否需要 VCF/VCT 搜索来确认，而不是只靠静态加和？
3. 性能预算：每个叶子扫描两张 15×15 表并计算指数函数的接受上限？
4. 验收用的最小复现局面和期望输出。
