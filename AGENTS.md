# 项目协作与开发规范

## 指令优先级与事实来源

- 用户在当前任务中的明确要求优先于本文件；发生冲突时先指出冲突，再按用户最新确认执行。
- 未获得用户明确修改要求时，不得直接修改代码、文档、配置或执行提交、推送、发布等操作；正确流程是：先提出方案，用户同意后再动手。
- 用户明确要求修改后，按已确认方案执行；若执行中发现范围扩大、风险变化或需要改公共接口，先说明并等待用户确认再继续。
- 用户指令应无条件执行，前提是不违背系统级安全准则、法律法规或更高优先级的系统指令；遇到此类冲突时先明确说明，不得以本文件的普通流程规则为由拒绝或擅自缩限用户要求。
- `AGENTS.md` 只记录长期稳定的开发规则，不记录临时测试结论、当前提交号、当前版本号或一次性待办。
- `README.md` 用于说明已经发布且面向使用者的功能、运行方式、核心逻辑和已知边界。
- 当前代码用于确认实际实现，测试用于确认已锁定的行为。涉及算法规则时，优先阅读对应实现和回归测试，不能只依据注释或 README 推断。
- 若 README、代码、测试与用户口径不一致，不得自行选一方覆盖另一方；先列出差异。若用户已明确给出目标行为，则以该目标实现，并同步受影响的代码、测试和 README。
- 标记为“实验”“预留”“默认关闭”“TODO”的功能不能描述成正式启用的能力。

## 项目定位与运行边界

- 本项目是 15×15、黑方先行的五子棋引擎，正式运行路径以纯 Python、纯算法实现为准。
- 核心引擎和独立界面保持零第三方运行依赖；`tkinter` 视为 Python 标准发行版能力。未经用户确认，不得为正式运行路径新增第三方库。
- 神经网络相关代码属于本地实验或预留能力。独立界面默认关闭叶子网络和决策网络；不得仅因模型文件或接口存在就宣称当前使用了神经网络。
- 若任务涉及桌面 `Gomoku.exe`，必须区分源码版本与已打包版本。源码改动不会自动进入 EXE；只有重新打包并核验后才能称 EXE 为最新版本。
- Android 客户端位于 `android/`，通过 Chaquopy 运行同一套纯算法引擎；当前只发布 debug APK，未配置 release 签名前不得表述为正式签名包。
- GitHub 仓库包含受跟踪的 Python 引擎、独立版源码、`android/` 客户端和发布脚本；本地插件层、模型、训练数据和实验产物是否入库，以 `.gitignore` 和用户明确指令为准。
- EXE、APK、macOS zip 只作为 GitHub Release 资产分发，不作为受跟踪源码入库。
- 正式发布产物以 GitHub Actions 构建并发布到 GitHub Release 的资产为准；日常使用、分发和验证优先直接从 Release 下载，不在本地重复构建。
- 本地构建只用于复现问题、诊断发布流程或用户明确要求，不得把本地产物当作正式发布版本。
- 从 Release 下载资产后必须用同版本的 `SHA256SUMS.txt` 校验；校验失败不得使用或分发。

## 模块职责

- `gomoku/core/pattern.py`：棋型扫描、18 子类识别和状态表基础能力。
- `gomoku/core/search.py`：黑白状态表、局部增量更新、战术候选、普通候选和攻防档位。
- `gomoku/core/deep_search.py`：温度银行递归、分支权重、剪枝、叶子评分、强制链五态判定和候选排序。
- `gomoku/core/engine.py`：组织威胁分析、候选生成、深推和最终选点，对外输出 `analyze_turn` 结果。
- `gomoku/game.py`、`gomoku/session.py`、`gomoku/service.py`：棋局规则、会话流程和引擎服务边界。
- `gomoku/memory.py`：对局记录、结算、棋谱经验和对称归一化。
- `play.py`、`play_cli.py`：独立图形界面和命令行入口；不得在入口中复制一套与核心引擎不同的选点逻辑。
- `android/`：Kotlin + Compose + Chaquopy Android 客户端；`app/src/main/python/gomoku/` 由同步脚本生成，不得手工编辑。
- `scripts/sync_android_engine.py`：把桌面 `gomoku/` 同步到 Android 内嵌引擎，并提供 `--check` 供 CI 使用。
- `.github/workflows/`：CI 与发布流程；构建产物不入库。
- `gomoku/adapter/`：插件或进程调用适配层，应复用核心引擎并保持会话隔离。
- `gomoku/tests/`：正式回归测试。修复已确认的 Bug 时，必须增加能在旧实现失败、在新实现通过的测试。

修改功能时应把逻辑放在所属模块，不跨层复制规则。若必须改变模块职责或公共返回结构，应先说明调用方影响。

## 通用开发原则

- 先复现或定位问题，再修改最小必要范围；不要顺手重构无关模块。
- 保持黑白双方逻辑对称。除明确的黑先规则、开局规则或用户指定差异外，不得只修白方或只修黑方路径。
- 模拟落子必须同步更新棋盘和 `Search` 状态表，并使用 journal 成对回滚。函数返回前，棋盘、状态表、分支列表和上下文列表必须恢复到调用前状态。
- 已占点不得进入候选结果；空候选、唯一候选、直接成五、直接堵五和全部必败等边界都要保留合法落子行为。
- 不吞掉会改变棋局结果的异常。已有的可选网络加载失败可以回退纯算法，但回退状态必须可观察且不得伪装成网络成功。
- 不新增与需求无关的兼容层、配置开关、抽象类或隐式全局状态。确有必要时先说明原因。
- 不凭感觉修改棋型权重、温度参数、剪枝阈值或攻防档位；参数变化必须有目标场景、对照结果和回归验证。
- 处理脏工作区时保留用户已有修改，不覆盖、不回退、不删除无关文件。
- Android 内嵌 Python 引擎的唯一事实来源是仓库根目录 `gomoku/`；修改引擎后必须运行同步脚本，并保证 `--check` 通过。
- 发布产物（EXE/APK/macOS zip）和构建中间目录不得进入源码库；只由发布工作流上传到 GitHub Release。

## 深推与强制链不变量

以下规则是当前决策语义，修改 `deep_search.py`、候选排序或界面标签时必须同时满足：

- 强制状态统一使用当前决策方视角，用带符号整数编码：
  - `+1` 必胜、`-1` 必败：硬结论；
  - `+2` 存在必胜、`-2` 存在必败：软证据；
  - `0` 无：没有发现证据。
- 同一决策节点先走原硬规则：所有有效子分支都是硬必胜 → 节点硬必胜；所有有效子分支都是硬必败 → 节点硬必败。
- 其余情况下走传播规则：任一有效子分支为硬必败或存在必败 → 节点为存在必败（败势证据优先于胜势证据）；否则任一有效子分支为硬必胜或存在必胜 → 节点为存在必胜；否则为无。
- 存在必败只传播风险，不能升级成硬必败；只有全部子分支硬必败时才保留硬必败。
- 例如 `[-1, -2, -1]` 必须聚合为 `-2`，不能升级成 `-1`。
- 最终候选状态只取候选根节点的聚合结论；深层某个子节点的状态不能直接当成候选根节点结论。
- 最终排序层级固定为：`必胜 > 存在必胜 > 无 > 存在必败 > 必败`；同一档内按候选分降序。
- 只要存在非必败候选，就不得选择必败候选；若所有合法候选均为败势档，按上述状态顺序和候选分选择兜底着法，保证程序能够继续落子。
- `state`、`state_name`、`child_state` 与 `forced` 必须来自同一套五态判定；`score` 仍由叶子评估和加权回溯产生，不得混入状态分。
- 必胜/必败只针对本次实际展开且未被剪掉的搜索树。代码、界面和文档不得把它表述为全盘穷举的数学证明。
- 修改上述任一规则时，必须同时覆盖 `_merge_child_states`、`_pick_key_state`、根节点状态计算和最终排序的相关测试。

## 候选、叶子评估与界面一致性

- `pattern -> search -> deep_search -> engine` 是正式决策链；入口和适配层不得绕过它另选棋。
- 成五、堵成五等绝对战术可以直取；VCF、双三、防守点和普通多候选应按当前引擎规则进入深推，不得因列表/字典转换丢失候选权重。
- 防守候选的权重会影响温度分配，必须保留原始权重语义。禁止把带权重字典静默转成统一权重列表。
- 叶子评分、五态强制状态和候选分是三个不同概念。不得用高分冒充必胜，也不得用深层状态直接覆盖根节点状态。
- 界面显示必须来自本次实际决策结果：
  - 叶子未触发时显示未触发；
  - 使用纯算法叶子时显示纯算法及实际叶子数；
  - 只有真实完成网络推理时才能显示神经网络；
  - 候选的五态显示必须与返回的 `state`、`state_name`、`child_state` 一致；`forced`、`fatal_ratio` 仅作兼容字段，不再参与排序。
- 后台思考不得直接操作 tkinter 控件；计算在线程中执行，界面更新回到主线程。

## 记忆、会话与适配器

- 不同会话必须保持棋盘、状态表、着法和结算互相隔离。
- 新对局和重建棋局时必须同步重置引擎真实着法与搜索状态，但保留明确要求保留的配置和全局记忆。
- 悔棋要同时恢复棋盘、引擎状态和终局结算；已经记账的终局被撤销后不能重复累计。
- 插件的常驻 Python 进程失败时可以使用既有回退路径，但旧进程的迟到退出不得破坏新请求。
- 修改 JSON 输出、`ranked` 字段或会话协议时，必须检查 Python 调用方、Node 插件桥接和独立界面三侧兼容性。

## 代码、注释与文档

- 新增或修改算法规则时，同步更新相关中文注释和 docstring；不得保留与实现相反的旧说明。
- 注释应解释决策语义、视角转换、权重来源、回滚原因和容易误改的边界，不为显而易见的赋值逐行加注释。
- 保持现有代码风格和公开接口，除非需求本身要求重构。内部辅助函数优先保持纯函数或静态方法，便于直接测试。
- 行列坐标在核心层使用零基 `(r, c)`；显示层使用 `A-O` 和 `1-15`。跨层转换必须明确，避免行列互换或重复加一。
- 程序逻辑、默认开关、界面能力、运行命令或已知限制变化时，同一次修改中更新 README 对应段落。
- README 只描述已经实现并验证的行为。性能数据和测试数量变化快，除非本次实际复测，否则不要新增或改写具体数字。
- 当前项目不声明 MIT 或其他许可证。未经用户明确指定，不得新增、恢复、替换许可证文件，也不得在 README、包元数据或源码头部填写许可证名称。

## 依赖、忽略文件与本地产物

- 安装新依赖、下载模型或引入外部代码前必须获得用户确认。
- 遵守 `.gitignore`。不得使用 `git add -f` 强行提交被忽略文件，除非用户明确要求将该类文件纳入仓库。
- PyInstaller、Android Gradle/Chaquopy 属于构建期依赖，版本固定在 workflow 中；正式运行路径仍保持零第三方运行依赖。
- `android/.gradle/`、`android/**/build/`、`local.properties`、`*.apk`、`*.aab` 等本地或构建产物不得入库。
- `lib/`、`package.json`、Node 依赖、本地会话文件、`gomoku/nn/`、模型权重、训练样本、KataGo 数据、GA 检查点和本地存档默认不进入 GitHub。
- 不修改或清理训练数据、模型、存档和实验产物，除非任务明确要求且已核对准确目标。
- 临时文件放入系统临时目录或明确的临时子目录，不把调试输出留在源码目录。

## 测试与验收

按变更范围执行最小必要测试，并在准备发布前执行完整回归：

```bash
# 完整 Python 回归
python -m unittest discover -s gomoku/tests -v

# 深推、叶子与候选排序定向回归
python -m unittest gomoku.tests.test_leaf_value -v
python -m unittest gomoku.tests.test_deep_sync -v
python -m unittest gomoku.tests.test_rank_candidates -v

# 本地插件层存在时执行 Node 桥接回归
node --test gomoku/tests/test_plugin.cjs
```

- 纯文档修改至少运行 `git diff --check`，并核对文档与当前实现一致。
- 修改 Android 内嵌引擎后，必须运行 `python scripts/sync_android_engine.py --check`。
- Android 产物发布前至少核对 `aapt2 dump badging` 的 versionCode/versionName/ABI 和 `apksigner verify`；有条件时在模拟器或真机安装启动。
- Windows EXE 发布前必须核对包内 assets/GA 数据、确认未包含 `torch`/`numpy`/`gomoku.nn`，并做启动冒烟。
- CI 失败时优先获取完整运行日志；需要临时诊断时使用临时分支，通过公开 annotation 或 step summary 暴露关键错误，修复后移除诊断改动。
- 修改棋盘/状态表更新时，必须验证模拟深推后状态完全恢复。
- 修改选点逻辑时，至少覆盖黑白双方、直接战术、多候选排序、未判定分支和必败兜底。
- 修改 GUI 时，除自动化测试外应检查启动、窗口缩放、后台思考和候选显示；若未实际启动界面，要明确说明未做人工视觉验证。
- 测试失败时不得为了通过而降低断言、删除测试或扩大容错；先判断是实现错误、测试过期还是用户规则变化。
- 最终汇报区分“自动化测试通过”“人工界面检查完成”“EXE 已重新打包验证”，不能互相替代。

## 构建、发布与 CI

- `.github/workflows/ci.yml`：运行 Python 完整回归和 Android 引擎同步检查。
- `.github/workflows/build-windows.yml`、`build-android.yml`、`build-macos.yml`：分别构建 Windows EXE、Android debug APK、macOS zip；只上传 workflow artifact，也可被 release 工作流复用。
- `.github/workflows/release.yml`：由 `v*` 标签或手动触发；先解析 tag 和 versionCode，再验证源码、并行构建三平台；全部成功后才创建 draft Release、上传全部资产和 `SHA256SUMS.txt`，最后 publish。
- 版本号以 tag 为准；Android versionCode 规则为 `major*10000 + minor*100 + patch`。
- Windows EXE 使用 Python 3.14 + PyInstaller 6.22.2，并排除 `torch`/`numpy`/`gomoku.nn`；macOS 保持 Python 3.12 + PyInstaller 6.22.2。
- Android CI 使用 JDK 17、Android SDK 36、build-tools 36.0.0、Python 3.14；`CHAQUOPY_BUILD_PYTHON` 指向 CI 安装的 Python。
- 发布资产命名：`Gomoku-QifengXiaojing-<tag>-win64.exe`、`Gomoku-QifengXiaojing-<tag>-debug.apk`、`QifengXiaojing-<tag>-macos.zip`。
- 标签推送会触发统一 release 工作流；Release 必须等 Windows、macOS、Android 全部构建成功后才可正式发布，失败时只允许保留 draft。
- 构建脚本必须跨平台：Windows runner 控制台按 UTF-8 输出；文本比较按 LF 归一化，不依赖 CRLF/LF。
- CI 同步或校验不得依赖被 `.gitignore` 排除、只在本地存在的实验文件。
- `publish` job 不执行 checkout 时，所有 `gh release` 命令必须显式指定 `--repo "$GITHUB_REPOSITORY"`。
- Release 发布后必须核对三平台资产和 `SHA256SUMS.txt`；任一平台失败时只能保留 draft，不得发布不完整 Release。

## Git 与版本发布

- 未经用户要求，不主动提交、打标签、推送、创建 GitHub Release 或改写远端历史。
- 每次准备推送前先检查 `git status`、完整 diff、`git diff --check` 和相关测试，确保不夹带用户文件或本地产物。
- 只要一次推送包含程序逻辑改动，就必须更新语义版本并创建匹配的 Git 标签。
- Bug 修复：补丁号加一，例如 `1.0.0 -> 1.0.1`。
- 新增逻辑或大型逻辑修改：次版本号加一，补丁号归零，例如 `1.0.1 -> 1.1.0`。
- 主版本号只能在用户明确下达主版本发布指令时修改；不得自行从 `1.x.x` 升到 `2.0.0`。
- 纯文档、注释或其他不改变程序行为的修改不要求升级版本。
- 同一批变更同时含 Bug 修复和新增/大型逻辑时，按较高级别升级次版本号。
- 发布时保持受 Git 跟踪的版本说明与标签一致。`package.json` 当前被忽略，不能强制加入仓库充当版本来源。
- 标签使用 `vMAJOR.MINOR.PATCH` 格式并指向包含代码、测试和对应 README 的最终发布提交。
- 已推送标签原则上不可改写。若刚发布的标签含有明确错误，必须向用户说明影响并获得明确授权后才能移动或强制更新。
- 删除、移动或强制更新任何已推送标签前，必须逐项确认该标签没有 Release 或 Release 资产，并记录用户明确授权。
- 失败、未发布 Release 的临时标签在获得授权后应清理，避免仓库中留下与正式发布无关的孤立标签。
- 临时诊断分支和诊断改动在问题修复后应清理，不留在长期分支历史中。
- 提交信息应准确描述变更，不把文档修改写成逻辑升级，也不把有限搜索结果宣传成已证明算法。

## 安全与删除操作

- 删除、覆盖、批量移动文件前先解析并核对准确路径，只处理用户明确指定的目标。
- 不使用会清空工作区或用户修改的命令，例如 `git reset --hard`、无目标校验的递归删除或覆盖式 checkout。
- 未被 Git 跟踪的文件删除后通常无法通过 Git 恢复；执行前必须明确告知这一点。
- 不在输出、日志、提交或文档中暴露访问令牌、凭据、私有仓库内容或本地个人数据。
- Android release keystore、Windows 代码签名证书及其密码只放 GitHub Secrets 或本机安全位置，不得进入仓库、构建日志或 Release 资产。
